/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SkinningLootTemplate } from "./SkinningLootTemplate";
import { SkinningLootTemplateCountArgs } from "./SkinningLootTemplateCountArgs";
import { SkinningLootTemplateFindManyArgs } from "./SkinningLootTemplateFindManyArgs";
import { SkinningLootTemplateFindUniqueArgs } from "./SkinningLootTemplateFindUniqueArgs";
import { DeleteSkinningLootTemplateArgs } from "./DeleteSkinningLootTemplateArgs";
import { SkinningLootTemplateService } from "../skinningLootTemplate.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SkinningLootTemplate)
export class SkinningLootTemplateResolverBase {
  constructor(
    protected readonly service: SkinningLootTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SkinningLootTemplate",
    action: "read",
    possession: "any",
  })
  async _skinningLootTemplatesMeta(
    @graphql.Args() args: SkinningLootTemplateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SkinningLootTemplate])
  @nestAccessControl.UseRoles({
    resource: "SkinningLootTemplate",
    action: "read",
    possession: "any",
  })
  async skinningLootTemplates(
    @graphql.Args() args: SkinningLootTemplateFindManyArgs
  ): Promise<SkinningLootTemplate[]> {
    return this.service.skinningLootTemplates(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SkinningLootTemplate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SkinningLootTemplate",
    action: "read",
    possession: "own",
  })
  async skinningLootTemplate(
    @graphql.Args() args: SkinningLootTemplateFindUniqueArgs
  ): Promise<SkinningLootTemplate | null> {
    const result = await this.service.skinningLootTemplate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => SkinningLootTemplate)
  @nestAccessControl.UseRoles({
    resource: "SkinningLootTemplate",
    action: "delete",
    possession: "any",
  })
  async deleteSkinningLootTemplate(
    @graphql.Args() args: DeleteSkinningLootTemplateArgs
  ): Promise<SkinningLootTemplate | null> {
    try {
      return await this.service.deleteSkinningLootTemplate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
