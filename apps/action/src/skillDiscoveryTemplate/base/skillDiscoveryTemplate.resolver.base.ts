/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SkillDiscoveryTemplate } from "./SkillDiscoveryTemplate";
import { SkillDiscoveryTemplateCountArgs } from "./SkillDiscoveryTemplateCountArgs";
import { SkillDiscoveryTemplateFindManyArgs } from "./SkillDiscoveryTemplateFindManyArgs";
import { SkillDiscoveryTemplateFindUniqueArgs } from "./SkillDiscoveryTemplateFindUniqueArgs";
import { DeleteSkillDiscoveryTemplateArgs } from "./DeleteSkillDiscoveryTemplateArgs";
import { SkillDiscoveryTemplateService } from "../skillDiscoveryTemplate.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SkillDiscoveryTemplate)
export class SkillDiscoveryTemplateResolverBase {
  constructor(
    protected readonly service: SkillDiscoveryTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SkillDiscoveryTemplate",
    action: "read",
    possession: "any",
  })
  async _skillDiscoveryTemplatesMeta(
    @graphql.Args() args: SkillDiscoveryTemplateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SkillDiscoveryTemplate])
  @nestAccessControl.UseRoles({
    resource: "SkillDiscoveryTemplate",
    action: "read",
    possession: "any",
  })
  async skillDiscoveryTemplates(
    @graphql.Args() args: SkillDiscoveryTemplateFindManyArgs
  ): Promise<SkillDiscoveryTemplate[]> {
    return this.service.skillDiscoveryTemplates(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SkillDiscoveryTemplate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SkillDiscoveryTemplate",
    action: "read",
    possession: "own",
  })
  async skillDiscoveryTemplate(
    @graphql.Args() args: SkillDiscoveryTemplateFindUniqueArgs
  ): Promise<SkillDiscoveryTemplate | null> {
    const result = await this.service.skillDiscoveryTemplate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => SkillDiscoveryTemplate)
  @nestAccessControl.UseRoles({
    resource: "SkillDiscoveryTemplate",
    action: "delete",
    possession: "any",
  })
  async deleteSkillDiscoveryTemplate(
    @graphql.Args() args: DeleteSkillDiscoveryTemplateArgs
  ): Promise<SkillDiscoveryTemplate | null> {
    try {
      return await this.service.deleteSkillDiscoveryTemplate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
