/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SkillPerfectItemTemplate } from "./SkillPerfectItemTemplate";
import { SkillPerfectItemTemplateCountArgs } from "./SkillPerfectItemTemplateCountArgs";
import { SkillPerfectItemTemplateFindManyArgs } from "./SkillPerfectItemTemplateFindManyArgs";
import { SkillPerfectItemTemplateFindUniqueArgs } from "./SkillPerfectItemTemplateFindUniqueArgs";
import { DeleteSkillPerfectItemTemplateArgs } from "./DeleteSkillPerfectItemTemplateArgs";
import { SkillPerfectItemTemplateService } from "../skillPerfectItemTemplate.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SkillPerfectItemTemplate)
export class SkillPerfectItemTemplateResolverBase {
  constructor(
    protected readonly service: SkillPerfectItemTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SkillPerfectItemTemplate",
    action: "read",
    possession: "any",
  })
  async _skillPerfectItemTemplatesMeta(
    @graphql.Args() args: SkillPerfectItemTemplateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SkillPerfectItemTemplate])
  @nestAccessControl.UseRoles({
    resource: "SkillPerfectItemTemplate",
    action: "read",
    possession: "any",
  })
  async skillPerfectItemTemplates(
    @graphql.Args() args: SkillPerfectItemTemplateFindManyArgs
  ): Promise<SkillPerfectItemTemplate[]> {
    return this.service.skillPerfectItemTemplates(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SkillPerfectItemTemplate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SkillPerfectItemTemplate",
    action: "read",
    possession: "own",
  })
  async skillPerfectItemTemplate(
    @graphql.Args() args: SkillPerfectItemTemplateFindUniqueArgs
  ): Promise<SkillPerfectItemTemplate | null> {
    const result = await this.service.skillPerfectItemTemplate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => SkillPerfectItemTemplate)
  @nestAccessControl.UseRoles({
    resource: "SkillPerfectItemTemplate",
    action: "delete",
    possession: "any",
  })
  async deleteSkillPerfectItemTemplate(
    @graphql.Args() args: DeleteSkillPerfectItemTemplateArgs
  ): Promise<SkillPerfectItemTemplate | null> {
    try {
      return await this.service.deleteSkillPerfectItemTemplate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
