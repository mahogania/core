/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SmartScripts } from "./SmartScripts";
import { SmartScriptsCountArgs } from "./SmartScriptsCountArgs";
import { SmartScriptsFindManyArgs } from "./SmartScriptsFindManyArgs";
import { SmartScriptsFindUniqueArgs } from "./SmartScriptsFindUniqueArgs";
import { DeleteSmartScriptsArgs } from "./DeleteSmartScriptsArgs";
import { SmartScriptsService } from "../smartScripts.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SmartScripts)
export class SmartScriptsResolverBase {
  constructor(
    protected readonly service: SmartScriptsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SmartScripts",
    action: "read",
    possession: "any",
  })
  async _smartScriptsItemsMeta(
    @graphql.Args() args: SmartScriptsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SmartScripts])
  @nestAccessControl.UseRoles({
    resource: "SmartScripts",
    action: "read",
    possession: "any",
  })
  async smartScriptsItems(
    @graphql.Args() args: SmartScriptsFindManyArgs
  ): Promise<SmartScripts[]> {
    return this.service.smartScriptsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SmartScripts, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SmartScripts",
    action: "read",
    possession: "own",
  })
  async smartScripts(
    @graphql.Args() args: SmartScriptsFindUniqueArgs
  ): Promise<SmartScripts | null> {
    const result = await this.service.smartScripts(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => SmartScripts)
  @nestAccessControl.UseRoles({
    resource: "SmartScripts",
    action: "delete",
    possession: "any",
  })
  async deleteSmartScripts(
    @graphql.Args() args: DeleteSmartScriptsArgs
  ): Promise<SmartScripts | null> {
    try {
      return await this.service.deleteSmartScripts(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
