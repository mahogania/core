/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CreatureTemplateSpellService } from "../creatureTemplateSpell.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureTemplateSpellCreateInput } from "./CreatureTemplateSpellCreateInput";
import { CreatureTemplateSpell } from "./CreatureTemplateSpell";
import { CreatureTemplateSpellFindManyArgs } from "./CreatureTemplateSpellFindManyArgs";
import { CreatureTemplateSpellWhereUniqueInput } from "./CreatureTemplateSpellWhereUniqueInput";
import { CreatureTemplateSpellUpdateInput } from "./CreatureTemplateSpellUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CreatureTemplateSpellControllerBase {
  constructor(
    protected readonly service: CreatureTemplateSpellService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CreatureTemplateSpell })
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateSpell",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: CreatureTemplateSpellCreateInput,
  })
  async createCreatureTemplateSpell(
    @common.Body() data: CreatureTemplateSpellCreateInput
  ): Promise<CreatureTemplateSpell> {
    return await this.service.createCreatureTemplateSpell({
      data: data,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CreatureTemplateSpell] })
  @ApiNestedQuery(CreatureTemplateSpellFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateSpell",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async creatureTemplateSpells(
    @common.Req() request: Request
  ): Promise<CreatureTemplateSpell[]> {
    const args = plainToClass(CreatureTemplateSpellFindManyArgs, request.query);
    return this.service.creatureTemplateSpells({
      ...args,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CreatureTemplateSpell })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateSpell",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async creatureTemplateSpell(
    @common.Param() params: CreatureTemplateSpellWhereUniqueInput
  ): Promise<CreatureTemplateSpell | null> {
    const result = await this.service.creatureTemplateSpell({
      where: params,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CreatureTemplateSpell })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateSpell",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: CreatureTemplateSpellUpdateInput,
  })
  async updateCreatureTemplateSpell(
    @common.Param() params: CreatureTemplateSpellWhereUniqueInput,
    @common.Body() data: CreatureTemplateSpellUpdateInput
  ): Promise<CreatureTemplateSpell | null> {
    try {
      return await this.service.updateCreatureTemplateSpell({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CreatureTemplateSpell })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateSpell",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCreatureTemplateSpell(
    @common.Param() params: CreatureTemplateSpellWhereUniqueInput
  ): Promise<CreatureTemplateSpell | null> {
    try {
      return await this.service.deleteCreatureTemplateSpell({
        where: params,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
