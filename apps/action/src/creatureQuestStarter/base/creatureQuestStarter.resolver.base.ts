/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureQuestStarter } from "./CreatureQuestStarter";
import { CreatureQuestStarterCountArgs } from "./CreatureQuestStarterCountArgs";
import { CreatureQuestStarterFindManyArgs } from "./CreatureQuestStarterFindManyArgs";
import { CreatureQuestStarterFindUniqueArgs } from "./CreatureQuestStarterFindUniqueArgs";
import { DeleteCreatureQuestStarterArgs } from "./DeleteCreatureQuestStarterArgs";
import { CreatureQuestStarterService } from "../creatureQuestStarter.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureQuestStarter)
export class CreatureQuestStarterResolverBase {
  constructor(
    protected readonly service: CreatureQuestStarterService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureQuestStarter",
    action: "read",
    possession: "any",
  })
  async _creatureQuestStartersMeta(
    @graphql.Args() args: CreatureQuestStarterCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureQuestStarter])
  @nestAccessControl.UseRoles({
    resource: "CreatureQuestStarter",
    action: "read",
    possession: "any",
  })
  async creatureQuestStarters(
    @graphql.Args() args: CreatureQuestStarterFindManyArgs
  ): Promise<CreatureQuestStarter[]> {
    return this.service.creatureQuestStarters(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureQuestStarter, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureQuestStarter",
    action: "read",
    possession: "own",
  })
  async creatureQuestStarter(
    @graphql.Args() args: CreatureQuestStarterFindUniqueArgs
  ): Promise<CreatureQuestStarter | null> {
    const result = await this.service.creatureQuestStarter(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureQuestStarter)
  @nestAccessControl.UseRoles({
    resource: "CreatureQuestStarter",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureQuestStarter(
    @graphql.Args() args: DeleteCreatureQuestStarterArgs
  ): Promise<CreatureQuestStarter | null> {
    try {
      return await this.service.deleteCreatureQuestStarter(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
