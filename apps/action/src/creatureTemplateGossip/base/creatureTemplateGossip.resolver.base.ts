/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureTemplateGossip } from "./CreatureTemplateGossip";
import { CreatureTemplateGossipCountArgs } from "./CreatureTemplateGossipCountArgs";
import { CreatureTemplateGossipFindManyArgs } from "./CreatureTemplateGossipFindManyArgs";
import { CreatureTemplateGossipFindUniqueArgs } from "./CreatureTemplateGossipFindUniqueArgs";
import { DeleteCreatureTemplateGossipArgs } from "./DeleteCreatureTemplateGossipArgs";
import { CreatureTemplateGossipService } from "../creatureTemplateGossip.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureTemplateGossip)
export class CreatureTemplateGossipResolverBase {
  constructor(
    protected readonly service: CreatureTemplateGossipService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateGossip",
    action: "read",
    possession: "any",
  })
  async _creatureTemplateGossipsMeta(
    @graphql.Args() args: CreatureTemplateGossipCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureTemplateGossip])
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateGossip",
    action: "read",
    possession: "any",
  })
  async creatureTemplateGossips(
    @graphql.Args() args: CreatureTemplateGossipFindManyArgs
  ): Promise<CreatureTemplateGossip[]> {
    return this.service.creatureTemplateGossips(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureTemplateGossip, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateGossip",
    action: "read",
    possession: "own",
  })
  async creatureTemplateGossip(
    @graphql.Args() args: CreatureTemplateGossipFindUniqueArgs
  ): Promise<CreatureTemplateGossip | null> {
    const result = await this.service.creatureTemplateGossip(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureTemplateGossip)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateGossip",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureTemplateGossip(
    @graphql.Args() args: DeleteCreatureTemplateGossipArgs
  ): Promise<CreatureTemplateGossip | null> {
    try {
      return await this.service.deleteCreatureTemplateGossip(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
