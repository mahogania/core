/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureSummonGroups } from "./CreatureSummonGroups";
import { CreatureSummonGroupsCountArgs } from "./CreatureSummonGroupsCountArgs";
import { CreatureSummonGroupsFindManyArgs } from "./CreatureSummonGroupsFindManyArgs";
import { CreatureSummonGroupsFindUniqueArgs } from "./CreatureSummonGroupsFindUniqueArgs";
import { DeleteCreatureSummonGroupsArgs } from "./DeleteCreatureSummonGroupsArgs";
import { CreatureSummonGroupsService } from "../creatureSummonGroups.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureSummonGroups)
export class CreatureSummonGroupsResolverBase {
  constructor(
    protected readonly service: CreatureSummonGroupsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureSummonGroups",
    action: "read",
    possession: "any",
  })
  async _creatureSummonGroupsItemsMeta(
    @graphql.Args() args: CreatureSummonGroupsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureSummonGroups])
  @nestAccessControl.UseRoles({
    resource: "CreatureSummonGroups",
    action: "read",
    possession: "any",
  })
  async creatureSummonGroupsItems(
    @graphql.Args() args: CreatureSummonGroupsFindManyArgs
  ): Promise<CreatureSummonGroups[]> {
    return this.service.creatureSummonGroupsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureSummonGroups, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureSummonGroups",
    action: "read",
    possession: "own",
  })
  async creatureSummonGroups(
    @graphql.Args() args: CreatureSummonGroupsFindUniqueArgs
  ): Promise<CreatureSummonGroups | null> {
    const result = await this.service.creatureSummonGroups(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureSummonGroups)
  @nestAccessControl.UseRoles({
    resource: "CreatureSummonGroups",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureSummonGroups(
    @graphql.Args() args: DeleteCreatureSummonGroupsArgs
  ): Promise<CreatureSummonGroups | null> {
    try {
      return await this.service.deleteCreatureSummonGroups(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
