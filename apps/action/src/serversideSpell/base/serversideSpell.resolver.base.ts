/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ServersideSpell } from "./ServersideSpell";
import { ServersideSpellCountArgs } from "./ServersideSpellCountArgs";
import { ServersideSpellFindManyArgs } from "./ServersideSpellFindManyArgs";
import { ServersideSpellFindUniqueArgs } from "./ServersideSpellFindUniqueArgs";
import { DeleteServersideSpellArgs } from "./DeleteServersideSpellArgs";
import { ServersideSpellService } from "../serversideSpell.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ServersideSpell)
export class ServersideSpellResolverBase {
  constructor(
    protected readonly service: ServersideSpellService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ServersideSpell",
    action: "read",
    possession: "any",
  })
  async _serversideSpellsMeta(
    @graphql.Args() args: ServersideSpellCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ServersideSpell])
  @nestAccessControl.UseRoles({
    resource: "ServersideSpell",
    action: "read",
    possession: "any",
  })
  async serversideSpells(
    @graphql.Args() args: ServersideSpellFindManyArgs
  ): Promise<ServersideSpell[]> {
    return this.service.serversideSpells(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ServersideSpell, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ServersideSpell",
    action: "read",
    possession: "own",
  })
  async serversideSpell(
    @graphql.Args() args: ServersideSpellFindUniqueArgs
  ): Promise<ServersideSpell | null> {
    const result = await this.service.serversideSpell(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ServersideSpell)
  @nestAccessControl.UseRoles({
    resource: "ServersideSpell",
    action: "delete",
    possession: "any",
  })
  async deleteServersideSpell(
    @graphql.Args() args: DeleteServersideSpellArgs
  ): Promise<ServersideSpell | null> {
    try {
      return await this.service.deleteServersideSpell(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
