/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureTemplateResistance } from "./CreatureTemplateResistance";
import { CreatureTemplateResistanceCountArgs } from "./CreatureTemplateResistanceCountArgs";
import { CreatureTemplateResistanceFindManyArgs } from "./CreatureTemplateResistanceFindManyArgs";
import { CreatureTemplateResistanceFindUniqueArgs } from "./CreatureTemplateResistanceFindUniqueArgs";
import { DeleteCreatureTemplateResistanceArgs } from "./DeleteCreatureTemplateResistanceArgs";
import { CreatureTemplateResistanceService } from "../creatureTemplateResistance.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureTemplateResistance)
export class CreatureTemplateResistanceResolverBase {
  constructor(
    protected readonly service: CreatureTemplateResistanceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateResistance",
    action: "read",
    possession: "any",
  })
  async _creatureTemplateResistancesMeta(
    @graphql.Args() args: CreatureTemplateResistanceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureTemplateResistance])
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateResistance",
    action: "read",
    possession: "any",
  })
  async creatureTemplateResistances(
    @graphql.Args() args: CreatureTemplateResistanceFindManyArgs
  ): Promise<CreatureTemplateResistance[]> {
    return this.service.creatureTemplateResistances(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureTemplateResistance, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateResistance",
    action: "read",
    possession: "own",
  })
  async creatureTemplateResistance(
    @graphql.Args() args: CreatureTemplateResistanceFindUniqueArgs
  ): Promise<CreatureTemplateResistance | null> {
    const result = await this.service.creatureTemplateResistance(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureTemplateResistance)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateResistance",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureTemplateResistance(
    @graphql.Args() args: DeleteCreatureTemplateResistanceArgs
  ): Promise<CreatureTemplateResistance | null> {
    try {
      return await this.service.deleteCreatureTemplateResistance(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
