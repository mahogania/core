/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureTemplateLocale } from "./CreatureTemplateLocale";
import { CreatureTemplateLocaleCountArgs } from "./CreatureTemplateLocaleCountArgs";
import { CreatureTemplateLocaleFindManyArgs } from "./CreatureTemplateLocaleFindManyArgs";
import { CreatureTemplateLocaleFindUniqueArgs } from "./CreatureTemplateLocaleFindUniqueArgs";
import { DeleteCreatureTemplateLocaleArgs } from "./DeleteCreatureTemplateLocaleArgs";
import { CreatureTemplateLocaleService } from "../creatureTemplateLocale.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureTemplateLocale)
export class CreatureTemplateLocaleResolverBase {
  constructor(
    protected readonly service: CreatureTemplateLocaleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateLocale",
    action: "read",
    possession: "any",
  })
  async _creatureTemplateLocalesMeta(
    @graphql.Args() args: CreatureTemplateLocaleCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureTemplateLocale])
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateLocale",
    action: "read",
    possession: "any",
  })
  async creatureTemplateLocales(
    @graphql.Args() args: CreatureTemplateLocaleFindManyArgs
  ): Promise<CreatureTemplateLocale[]> {
    return this.service.creatureTemplateLocales(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureTemplateLocale, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateLocale",
    action: "read",
    possession: "own",
  })
  async creatureTemplateLocale(
    @graphql.Args() args: CreatureTemplateLocaleFindUniqueArgs
  ): Promise<CreatureTemplateLocale | null> {
    const result = await this.service.creatureTemplateLocale(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureTemplateLocale)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateLocale",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureTemplateLocale(
    @graphql.Args() args: DeleteCreatureTemplateLocaleArgs
  ): Promise<CreatureTemplateLocale | null> {
    try {
      return await this.service.deleteCreatureTemplateLocale(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
