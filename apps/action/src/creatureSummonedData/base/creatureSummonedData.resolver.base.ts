/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureSummonedData } from "./CreatureSummonedData";
import { CreatureSummonedDataCountArgs } from "./CreatureSummonedDataCountArgs";
import { CreatureSummonedDataFindManyArgs } from "./CreatureSummonedDataFindManyArgs";
import { CreatureSummonedDataFindUniqueArgs } from "./CreatureSummonedDataFindUniqueArgs";
import { DeleteCreatureSummonedDataArgs } from "./DeleteCreatureSummonedDataArgs";
import { CreatureSummonedDataService } from "../creatureSummonedData.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureSummonedData)
export class CreatureSummonedDataResolverBase {
  constructor(
    protected readonly service: CreatureSummonedDataService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureSummonedData",
    action: "read",
    possession: "any",
  })
  async _creatureSummonedDataItemsMeta(
    @graphql.Args() args: CreatureSummonedDataCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureSummonedData])
  @nestAccessControl.UseRoles({
    resource: "CreatureSummonedData",
    action: "read",
    possession: "any",
  })
  async creatureSummonedDataItems(
    @graphql.Args() args: CreatureSummonedDataFindManyArgs
  ): Promise<CreatureSummonedData[]> {
    return this.service.creatureSummonedDataItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureSummonedData, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureSummonedData",
    action: "read",
    possession: "own",
  })
  async creatureSummonedData(
    @graphql.Args() args: CreatureSummonedDataFindUniqueArgs
  ): Promise<CreatureSummonedData | null> {
    const result = await this.service.creatureSummonedData(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureSummonedData)
  @nestAccessControl.UseRoles({
    resource: "CreatureSummonedData",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureSummonedData(
    @graphql.Args() args: DeleteCreatureSummonedDataArgs
  ): Promise<CreatureSummonedData | null> {
    try {
      return await this.service.deleteCreatureSummonedData(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
