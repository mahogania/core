/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureTemplateDifficulty } from "./CreatureTemplateDifficulty";
import { CreatureTemplateDifficultyCountArgs } from "./CreatureTemplateDifficultyCountArgs";
import { CreatureTemplateDifficultyFindManyArgs } from "./CreatureTemplateDifficultyFindManyArgs";
import { CreatureTemplateDifficultyFindUniqueArgs } from "./CreatureTemplateDifficultyFindUniqueArgs";
import { DeleteCreatureTemplateDifficultyArgs } from "./DeleteCreatureTemplateDifficultyArgs";
import { CreatureTemplateDifficultyService } from "../creatureTemplateDifficulty.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureTemplateDifficulty)
export class CreatureTemplateDifficultyResolverBase {
  constructor(
    protected readonly service: CreatureTemplateDifficultyService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateDifficulty",
    action: "read",
    possession: "any",
  })
  async _creatureTemplateDifficultiesMeta(
    @graphql.Args() args: CreatureTemplateDifficultyCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureTemplateDifficulty])
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateDifficulty",
    action: "read",
    possession: "any",
  })
  async creatureTemplateDifficulties(
    @graphql.Args() args: CreatureTemplateDifficultyFindManyArgs
  ): Promise<CreatureTemplateDifficulty[]> {
    return this.service.creatureTemplateDifficulties(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureTemplateDifficulty, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateDifficulty",
    action: "read",
    possession: "own",
  })
  async creatureTemplateDifficulty(
    @graphql.Args() args: CreatureTemplateDifficultyFindUniqueArgs
  ): Promise<CreatureTemplateDifficulty | null> {
    const result = await this.service.creatureTemplateDifficulty(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureTemplateDifficulty)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateDifficulty",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureTemplateDifficulty(
    @graphql.Args() args: DeleteCreatureTemplateDifficultyArgs
  ): Promise<CreatureTemplateDifficulty | null> {
    try {
      return await this.service.deleteCreatureTemplateDifficulty(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
