/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { SpawnGroupTemplate } from "./SpawnGroupTemplate";
import { SpawnGroupTemplateCountArgs } from "./SpawnGroupTemplateCountArgs";
import { SpawnGroupTemplateFindManyArgs } from "./SpawnGroupTemplateFindManyArgs";
import { SpawnGroupTemplateFindUniqueArgs } from "./SpawnGroupTemplateFindUniqueArgs";
import { DeleteSpawnGroupTemplateArgs } from "./DeleteSpawnGroupTemplateArgs";
import { SpawnGroupTemplateService } from "../spawnGroupTemplate.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => SpawnGroupTemplate)
export class SpawnGroupTemplateResolverBase {
  constructor(
    protected readonly service: SpawnGroupTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "SpawnGroupTemplate",
    action: "read",
    possession: "any",
  })
  async _spawnGroupTemplatesMeta(
    @graphql.Args() args: SpawnGroupTemplateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [SpawnGroupTemplate])
  @nestAccessControl.UseRoles({
    resource: "SpawnGroupTemplate",
    action: "read",
    possession: "any",
  })
  async spawnGroupTemplates(
    @graphql.Args() args: SpawnGroupTemplateFindManyArgs
  ): Promise<SpawnGroupTemplate[]> {
    return this.service.spawnGroupTemplates(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => SpawnGroupTemplate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "SpawnGroupTemplate",
    action: "read",
    possession: "own",
  })
  async spawnGroupTemplate(
    @graphql.Args() args: SpawnGroupTemplateFindUniqueArgs
  ): Promise<SpawnGroupTemplate | null> {
    const result = await this.service.spawnGroupTemplate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => SpawnGroupTemplate)
  @nestAccessControl.UseRoles({
    resource: "SpawnGroupTemplate",
    action: "delete",
    possession: "any",
  })
  async deleteSpawnGroupTemplate(
    @graphql.Args() args: DeleteSpawnGroupTemplateArgs
  ): Promise<SpawnGroupTemplate | null> {
    try {
      return await this.service.deleteSpawnGroupTemplate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
