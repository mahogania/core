/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureText } from "./CreatureText";
import { CreatureTextCountArgs } from "./CreatureTextCountArgs";
import { CreatureTextFindManyArgs } from "./CreatureTextFindManyArgs";
import { CreatureTextFindUniqueArgs } from "./CreatureTextFindUniqueArgs";
import { DeleteCreatureTextArgs } from "./DeleteCreatureTextArgs";
import { CreatureTextService } from "../creatureText.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureText)
export class CreatureTextResolverBase {
  constructor(
    protected readonly service: CreatureTextService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureText",
    action: "read",
    possession: "any",
  })
  async _creatureTextsMeta(
    @graphql.Args() args: CreatureTextCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureText])
  @nestAccessControl.UseRoles({
    resource: "CreatureText",
    action: "read",
    possession: "any",
  })
  async creatureTexts(
    @graphql.Args() args: CreatureTextFindManyArgs
  ): Promise<CreatureText[]> {
    return this.service.creatureTexts(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureText, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureText",
    action: "read",
    possession: "own",
  })
  async creatureText(
    @graphql.Args() args: CreatureTextFindUniqueArgs
  ): Promise<CreatureText | null> {
    const result = await this.service.creatureText(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureText)
  @nestAccessControl.UseRoles({
    resource: "CreatureText",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureText(
    @graphql.Args() args: DeleteCreatureTextArgs
  ): Promise<CreatureText | null> {
    try {
      return await this.service.deleteCreatureText(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
