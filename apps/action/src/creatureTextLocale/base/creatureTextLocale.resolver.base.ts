/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureTextLocale } from "./CreatureTextLocale";
import { CreatureTextLocaleCountArgs } from "./CreatureTextLocaleCountArgs";
import { CreatureTextLocaleFindManyArgs } from "./CreatureTextLocaleFindManyArgs";
import { CreatureTextLocaleFindUniqueArgs } from "./CreatureTextLocaleFindUniqueArgs";
import { DeleteCreatureTextLocaleArgs } from "./DeleteCreatureTextLocaleArgs";
import { CreatureTextLocaleService } from "../creatureTextLocale.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureTextLocale)
export class CreatureTextLocaleResolverBase {
  constructor(
    protected readonly service: CreatureTextLocaleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureTextLocale",
    action: "read",
    possession: "any",
  })
  async _creatureTextLocalesMeta(
    @graphql.Args() args: CreatureTextLocaleCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureTextLocale])
  @nestAccessControl.UseRoles({
    resource: "CreatureTextLocale",
    action: "read",
    possession: "any",
  })
  async creatureTextLocales(
    @graphql.Args() args: CreatureTextLocaleFindManyArgs
  ): Promise<CreatureTextLocale[]> {
    return this.service.creatureTextLocales(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureTextLocale, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureTextLocale",
    action: "read",
    possession: "own",
  })
  async creatureTextLocale(
    @graphql.Args() args: CreatureTextLocaleFindUniqueArgs
  ): Promise<CreatureTextLocale | null> {
    const result = await this.service.creatureTextLocale(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureTextLocale)
  @nestAccessControl.UseRoles({
    resource: "CreatureTextLocale",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureTextLocale(
    @graphql.Args() args: DeleteCreatureTextLocaleArgs
  ): Promise<CreatureTextLocale | null> {
    try {
      return await this.service.deleteCreatureTextLocale(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
