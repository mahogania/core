/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CreatureTemplateMovement } from "./CreatureTemplateMovement";
import { CreatureTemplateMovementCountArgs } from "./CreatureTemplateMovementCountArgs";
import { CreatureTemplateMovementFindManyArgs } from "./CreatureTemplateMovementFindManyArgs";
import { CreatureTemplateMovementFindUniqueArgs } from "./CreatureTemplateMovementFindUniqueArgs";
import { DeleteCreatureTemplateMovementArgs } from "./DeleteCreatureTemplateMovementArgs";
import { CreatureTemplateMovementService } from "../creatureTemplateMovement.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => CreatureTemplateMovement)
export class CreatureTemplateMovementResolverBase {
  constructor(
    protected readonly service: CreatureTemplateMovementService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateMovement",
    action: "read",
    possession: "any",
  })
  async _creatureTemplateMovementsMeta(
    @graphql.Args() args: CreatureTemplateMovementCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [CreatureTemplateMovement])
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateMovement",
    action: "read",
    possession: "any",
  })
  async creatureTemplateMovements(
    @graphql.Args() args: CreatureTemplateMovementFindManyArgs
  ): Promise<CreatureTemplateMovement[]> {
    return this.service.creatureTemplateMovements(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => CreatureTemplateMovement, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateMovement",
    action: "read",
    possession: "own",
  })
  async creatureTemplateMovement(
    @graphql.Args() args: CreatureTemplateMovementFindUniqueArgs
  ): Promise<CreatureTemplateMovement | null> {
    const result = await this.service.creatureTemplateMovement(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CreatureTemplateMovement)
  @nestAccessControl.UseRoles({
    resource: "CreatureTemplateMovement",
    action: "delete",
    possession: "any",
  })
  async deleteCreatureTemplateMovement(
    @graphql.Args() args: DeleteCreatureTemplateMovementArgs
  ): Promise<CreatureTemplateMovement | null> {
    try {
      return await this.service.deleteCreatureTemplateMovement(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
