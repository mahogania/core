/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayerchoiceResponseLocale } from "./PlayerchoiceResponseLocale";
import { PlayerchoiceResponseLocaleCountArgs } from "./PlayerchoiceResponseLocaleCountArgs";
import { PlayerchoiceResponseLocaleFindManyArgs } from "./PlayerchoiceResponseLocaleFindManyArgs";
import { PlayerchoiceResponseLocaleFindUniqueArgs } from "./PlayerchoiceResponseLocaleFindUniqueArgs";
import { DeletePlayerchoiceResponseLocaleArgs } from "./DeletePlayerchoiceResponseLocaleArgs";
import { PlayerchoiceResponseLocaleService } from "../playerchoiceResponseLocale.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayerchoiceResponseLocale)
export class PlayerchoiceResponseLocaleResolverBase {
  constructor(
    protected readonly service: PlayerchoiceResponseLocaleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseLocale",
    action: "read",
    possession: "any",
  })
  async _playerchoiceResponseLocalesMeta(
    @graphql.Args() args: PlayerchoiceResponseLocaleCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayerchoiceResponseLocale])
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseLocale",
    action: "read",
    possession: "any",
  })
  async playerchoiceResponseLocales(
    @graphql.Args() args: PlayerchoiceResponseLocaleFindManyArgs
  ): Promise<PlayerchoiceResponseLocale[]> {
    return this.service.playerchoiceResponseLocales(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayerchoiceResponseLocale, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseLocale",
    action: "read",
    possession: "own",
  })
  async playerchoiceResponseLocale(
    @graphql.Args() args: PlayerchoiceResponseLocaleFindUniqueArgs
  ): Promise<PlayerchoiceResponseLocale | null> {
    const result = await this.service.playerchoiceResponseLocale(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayerchoiceResponseLocale)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseLocale",
    action: "delete",
    possession: "any",
  })
  async deletePlayerchoiceResponseLocale(
    @graphql.Args() args: DeletePlayerchoiceResponseLocaleArgs
  ): Promise<PlayerchoiceResponseLocale | null> {
    try {
      return await this.service.deletePlayerchoiceResponseLocale(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
