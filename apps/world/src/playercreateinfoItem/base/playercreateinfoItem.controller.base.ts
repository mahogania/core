/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PlayercreateinfoItemService } from "../playercreateinfoItem.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayercreateinfoItemCreateInput } from "./PlayercreateinfoItemCreateInput";
import { PlayercreateinfoItem } from "./PlayercreateinfoItem";
import { PlayercreateinfoItemFindManyArgs } from "./PlayercreateinfoItemFindManyArgs";
import { PlayercreateinfoItemWhereUniqueInput } from "./PlayercreateinfoItemWhereUniqueInput";
import { PlayercreateinfoItemUpdateInput } from "./PlayercreateinfoItemUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PlayercreateinfoItemControllerBase {
  constructor(
    protected readonly service: PlayercreateinfoItemService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PlayercreateinfoItem })
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoItem",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: PlayercreateinfoItemCreateInput,
  })
  async createPlayercreateinfoItem(
    @common.Body() data: PlayercreateinfoItemCreateInput
  ): Promise<PlayercreateinfoItem> {
    return await this.service.createPlayercreateinfoItem({
      data: data,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PlayercreateinfoItem] })
  @ApiNestedQuery(PlayercreateinfoItemFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoItem",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async playercreateinfoItems(
    @common.Req() request: Request
  ): Promise<PlayercreateinfoItem[]> {
    const args = plainToClass(PlayercreateinfoItemFindManyArgs, request.query);
    return this.service.playercreateinfoItems({
      ...args,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PlayercreateinfoItem })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoItem",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async playercreateinfoItem(
    @common.Param() params: PlayercreateinfoItemWhereUniqueInput
  ): Promise<PlayercreateinfoItem | null> {
    const result = await this.service.playercreateinfoItem({
      where: params,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PlayercreateinfoItem })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoItem",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: PlayercreateinfoItemUpdateInput,
  })
  async updatePlayercreateinfoItem(
    @common.Param() params: PlayercreateinfoItemWhereUniqueInput,
    @common.Body() data: PlayercreateinfoItemUpdateInput
  ): Promise<PlayercreateinfoItem | null> {
    try {
      return await this.service.updatePlayercreateinfoItem({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PlayercreateinfoItem })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoItem",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePlayercreateinfoItem(
    @common.Param() params: PlayercreateinfoItemWhereUniqueInput
  ): Promise<PlayercreateinfoItem | null> {
    try {
      return await this.service.deletePlayercreateinfoItem({
        where: params,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
