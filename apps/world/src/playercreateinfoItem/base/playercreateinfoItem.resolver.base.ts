/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayercreateinfoItem } from "./PlayercreateinfoItem";
import { PlayercreateinfoItemCountArgs } from "./PlayercreateinfoItemCountArgs";
import { PlayercreateinfoItemFindManyArgs } from "./PlayercreateinfoItemFindManyArgs";
import { PlayercreateinfoItemFindUniqueArgs } from "./PlayercreateinfoItemFindUniqueArgs";
import { DeletePlayercreateinfoItemArgs } from "./DeletePlayercreateinfoItemArgs";
import { PlayercreateinfoItemService } from "../playercreateinfoItem.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayercreateinfoItem)
export class PlayercreateinfoItemResolverBase {
  constructor(
    protected readonly service: PlayercreateinfoItemService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoItem",
    action: "read",
    possession: "any",
  })
  async _playercreateinfoItemsMeta(
    @graphql.Args() args: PlayercreateinfoItemCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayercreateinfoItem])
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoItem",
    action: "read",
    possession: "any",
  })
  async playercreateinfoItems(
    @graphql.Args() args: PlayercreateinfoItemFindManyArgs
  ): Promise<PlayercreateinfoItem[]> {
    return this.service.playercreateinfoItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayercreateinfoItem, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoItem",
    action: "read",
    possession: "own",
  })
  async playercreateinfoItem(
    @graphql.Args() args: PlayercreateinfoItemFindUniqueArgs
  ): Promise<PlayercreateinfoItem | null> {
    const result = await this.service.playercreateinfoItem(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayercreateinfoItem)
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoItem",
    action: "delete",
    possession: "any",
  })
  async deletePlayercreateinfoItem(
    @graphql.Args() args: DeletePlayercreateinfoItemArgs
  ): Promise<PlayercreateinfoItem | null> {
    try {
      return await this.service.deletePlayercreateinfoItem(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
