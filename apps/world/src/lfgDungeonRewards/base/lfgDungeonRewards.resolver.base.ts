/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { LfgDungeonRewards } from "./LfgDungeonRewards";
import { LfgDungeonRewardsCountArgs } from "./LfgDungeonRewardsCountArgs";
import { LfgDungeonRewardsFindManyArgs } from "./LfgDungeonRewardsFindManyArgs";
import { LfgDungeonRewardsFindUniqueArgs } from "./LfgDungeonRewardsFindUniqueArgs";
import { DeleteLfgDungeonRewardsArgs } from "./DeleteLfgDungeonRewardsArgs";
import { LfgDungeonRewardsService } from "../lfgDungeonRewards.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => LfgDungeonRewards)
export class LfgDungeonRewardsResolverBase {
  constructor(
    protected readonly service: LfgDungeonRewardsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "LfgDungeonRewards",
    action: "read",
    possession: "any",
  })
  async _lfgDungeonRewardsItemsMeta(
    @graphql.Args() args: LfgDungeonRewardsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [LfgDungeonRewards])
  @nestAccessControl.UseRoles({
    resource: "LfgDungeonRewards",
    action: "read",
    possession: "any",
  })
  async lfgDungeonRewardsItems(
    @graphql.Args() args: LfgDungeonRewardsFindManyArgs
  ): Promise<LfgDungeonRewards[]> {
    return this.service.lfgDungeonRewardsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => LfgDungeonRewards, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "LfgDungeonRewards",
    action: "read",
    possession: "own",
  })
  async lfgDungeonRewards(
    @graphql.Args() args: LfgDungeonRewardsFindUniqueArgs
  ): Promise<LfgDungeonRewards | null> {
    const result = await this.service.lfgDungeonRewards(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => LfgDungeonRewards)
  @nestAccessControl.UseRoles({
    resource: "LfgDungeonRewards",
    action: "delete",
    possession: "any",
  })
  async deleteLfgDungeonRewards(
    @graphql.Args() args: DeleteLfgDungeonRewardsArgs
  ): Promise<LfgDungeonRewards | null> {
    try {
      return await this.service.deleteLfgDungeonRewards(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
