/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { Playerchoice } from "./Playerchoice";
import { PlayerchoiceCountArgs } from "./PlayerchoiceCountArgs";
import { PlayerchoiceFindManyArgs } from "./PlayerchoiceFindManyArgs";
import { PlayerchoiceFindUniqueArgs } from "./PlayerchoiceFindUniqueArgs";
import { DeletePlayerchoiceArgs } from "./DeletePlayerchoiceArgs";
import { PlayerchoiceService } from "../playerchoice.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Playerchoice)
export class PlayerchoiceResolverBase {
  constructor(
    protected readonly service: PlayerchoiceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Playerchoice",
    action: "read",
    possession: "any",
  })
  async _playerchoicesMeta(
    @graphql.Args() args: PlayerchoiceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Playerchoice])
  @nestAccessControl.UseRoles({
    resource: "Playerchoice",
    action: "read",
    possession: "any",
  })
  async playerchoices(
    @graphql.Args() args: PlayerchoiceFindManyArgs
  ): Promise<Playerchoice[]> {
    return this.service.playerchoices(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Playerchoice, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Playerchoice",
    action: "read",
    possession: "own",
  })
  async playerchoice(
    @graphql.Args() args: PlayerchoiceFindUniqueArgs
  ): Promise<Playerchoice | null> {
    const result = await this.service.playerchoice(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Playerchoice)
  @nestAccessControl.UseRoles({
    resource: "Playerchoice",
    action: "delete",
    possession: "any",
  })
  async deletePlayerchoice(
    @graphql.Args() args: DeletePlayerchoiceArgs
  ): Promise<Playerchoice | null> {
    try {
      return await this.service.deletePlayerchoice(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
