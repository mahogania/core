/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { GrpcMethod } from "@nestjs/microservices";
import { FishingLootTemplateService } from "../fishingLootTemplate.service";
import { FishingLootTemplateCreateInput } from "./FishingLootTemplateCreateInput";
import { FishingLootTemplateWhereInput } from "./FishingLootTemplateWhereInput";
import { FishingLootTemplateWhereUniqueInput } from "./FishingLootTemplateWhereUniqueInput";
import { FishingLootTemplateFindManyArgs } from "./FishingLootTemplateFindManyArgs";
import { FishingLootTemplateUpdateInput } from "./FishingLootTemplateUpdateInput";
import { FishingLootTemplate } from "./FishingLootTemplate";

export class FishingLootTemplateGrpcControllerBase {
  constructor(protected readonly service: FishingLootTemplateService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: FishingLootTemplate })
  @GrpcMethod("FishingLootTemplateService", "createFishingLootTemplate")
  async createFishingLootTemplate(
    @common.Body() data: FishingLootTemplateCreateInput
  ): Promise<FishingLootTemplate> {
    return await this.service.createFishingLootTemplate({
      data: data,
      select: {
        chance: true,
        comment: true,
        createdAt: true,
        entry: true,
        groupId: true,
        id: true,
        item: true,
        lootMode: true,
        maxCount: true,
        minCount: true,
        questRequired: true,
        reference: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [FishingLootTemplate] })
  @ApiNestedQuery(FishingLootTemplateFindManyArgs)
  @GrpcMethod("FishingLootTemplateService", "fishingLootTemplates")
  async fishingLootTemplates(
    @common.Req() request: Request
  ): Promise<FishingLootTemplate[]> {
    const args = plainToClass(FishingLootTemplateFindManyArgs, request.query);
    return this.service.fishingLootTemplates({
      ...args,
      select: {
        chance: true,
        comment: true,
        createdAt: true,
        entry: true,
        groupId: true,
        id: true,
        item: true,
        lootMode: true,
        maxCount: true,
        minCount: true,
        questRequired: true,
        reference: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: FishingLootTemplate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("FishingLootTemplateService", "fishingLootTemplate")
  async fishingLootTemplate(
    @common.Param() params: FishingLootTemplateWhereUniqueInput
  ): Promise<FishingLootTemplate | null> {
    const result = await this.service.fishingLootTemplate({
      where: params,
      select: {
        chance: true,
        comment: true,
        createdAt: true,
        entry: true,
        groupId: true,
        id: true,
        item: true,
        lootMode: true,
        maxCount: true,
        minCount: true,
        questRequired: true,
        reference: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: FishingLootTemplate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("FishingLootTemplateService", "updateFishingLootTemplate")
  async updateFishingLootTemplate(
    @common.Param() params: FishingLootTemplateWhereUniqueInput,
    @common.Body() data: FishingLootTemplateUpdateInput
  ): Promise<FishingLootTemplate | null> {
    try {
      return await this.service.updateFishingLootTemplate({
        where: params,
        data: data,
        select: {
          chance: true,
          comment: true,
          createdAt: true,
          entry: true,
          groupId: true,
          id: true,
          item: true,
          lootMode: true,
          maxCount: true,
          minCount: true,
          questRequired: true,
          reference: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: FishingLootTemplate })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("FishingLootTemplateService", "deleteFishingLootTemplate")
  async deleteFishingLootTemplate(
    @common.Param() params: FishingLootTemplateWhereUniqueInput
  ): Promise<FishingLootTemplate | null> {
    try {
      return await this.service.deleteFishingLootTemplate({
        where: params,
        select: {
          chance: true,
          comment: true,
          createdAt: true,
          entry: true,
          groupId: true,
          id: true,
          item: true,
          lootMode: true,
          maxCount: true,
          minCount: true,
          questRequired: true,
          reference: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
