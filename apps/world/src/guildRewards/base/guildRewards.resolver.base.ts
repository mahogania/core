/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { GuildRewards } from "./GuildRewards";
import { GuildRewardsCountArgs } from "./GuildRewardsCountArgs";
import { GuildRewardsFindManyArgs } from "./GuildRewardsFindManyArgs";
import { GuildRewardsFindUniqueArgs } from "./GuildRewardsFindUniqueArgs";
import { DeleteGuildRewardsArgs } from "./DeleteGuildRewardsArgs";
import { GuildRewardsService } from "../guildRewards.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => GuildRewards)
export class GuildRewardsResolverBase {
  constructor(
    protected readonly service: GuildRewardsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "GuildRewards",
    action: "read",
    possession: "any",
  })
  async _guildRewardsItemsMeta(
    @graphql.Args() args: GuildRewardsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [GuildRewards])
  @nestAccessControl.UseRoles({
    resource: "GuildRewards",
    action: "read",
    possession: "any",
  })
  async guildRewardsItems(
    @graphql.Args() args: GuildRewardsFindManyArgs
  ): Promise<GuildRewards[]> {
    return this.service.guildRewardsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => GuildRewards, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "GuildRewards",
    action: "read",
    possession: "own",
  })
  async guildRewards(
    @graphql.Args() args: GuildRewardsFindUniqueArgs
  ): Promise<GuildRewards | null> {
    const result = await this.service.guildRewards(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => GuildRewards)
  @nestAccessControl.UseRoles({
    resource: "GuildRewards",
    action: "delete",
    possession: "any",
  })
  async deleteGuildRewards(
    @graphql.Args() args: DeleteGuildRewardsArgs
  ): Promise<GuildRewards | null> {
    try {
      return await this.service.deleteGuildRewards(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
