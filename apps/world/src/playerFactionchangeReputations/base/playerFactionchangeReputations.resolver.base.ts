/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayerFactionchangeReputations } from "./PlayerFactionchangeReputations";
import { PlayerFactionchangeReputationsCountArgs } from "./PlayerFactionchangeReputationsCountArgs";
import { PlayerFactionchangeReputationsFindManyArgs } from "./PlayerFactionchangeReputationsFindManyArgs";
import { PlayerFactionchangeReputationsFindUniqueArgs } from "./PlayerFactionchangeReputationsFindUniqueArgs";
import { DeletePlayerFactionchangeReputationsArgs } from "./DeletePlayerFactionchangeReputationsArgs";
import { PlayerFactionchangeReputationsService } from "../playerFactionchangeReputations.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayerFactionchangeReputations)
export class PlayerFactionchangeReputationsResolverBase {
  constructor(
    protected readonly service: PlayerFactionchangeReputationsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayerFactionchangeReputations",
    action: "read",
    possession: "any",
  })
  async _playerFactionchangeReputationsItemsMeta(
    @graphql.Args() args: PlayerFactionchangeReputationsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayerFactionchangeReputations])
  @nestAccessControl.UseRoles({
    resource: "PlayerFactionchangeReputations",
    action: "read",
    possession: "any",
  })
  async playerFactionchangeReputationsItems(
    @graphql.Args() args: PlayerFactionchangeReputationsFindManyArgs
  ): Promise<PlayerFactionchangeReputations[]> {
    return this.service.playerFactionchangeReputationsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayerFactionchangeReputations, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayerFactionchangeReputations",
    action: "read",
    possession: "own",
  })
  async playerFactionchangeReputations(
    @graphql.Args() args: PlayerFactionchangeReputationsFindUniqueArgs
  ): Promise<PlayerFactionchangeReputations | null> {
    const result = await this.service.playerFactionchangeReputations(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayerFactionchangeReputations)
  @nestAccessControl.UseRoles({
    resource: "PlayerFactionchangeReputations",
    action: "delete",
    possession: "any",
  })
  async deletePlayerFactionchangeReputations(
    @graphql.Args() args: DeletePlayerFactionchangeReputationsArgs
  ): Promise<PlayerFactionchangeReputations | null> {
    try {
      return await this.service.deletePlayerFactionchangeReputations(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
