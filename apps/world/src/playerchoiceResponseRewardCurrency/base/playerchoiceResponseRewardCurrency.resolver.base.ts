/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayerchoiceResponseRewardCurrency } from "./PlayerchoiceResponseRewardCurrency";
import { PlayerchoiceResponseRewardCurrencyCountArgs } from "./PlayerchoiceResponseRewardCurrencyCountArgs";
import { PlayerchoiceResponseRewardCurrencyFindManyArgs } from "./PlayerchoiceResponseRewardCurrencyFindManyArgs";
import { PlayerchoiceResponseRewardCurrencyFindUniqueArgs } from "./PlayerchoiceResponseRewardCurrencyFindUniqueArgs";
import { DeletePlayerchoiceResponseRewardCurrencyArgs } from "./DeletePlayerchoiceResponseRewardCurrencyArgs";
import { PlayerchoiceResponseRewardCurrencyService } from "../playerchoiceResponseRewardCurrency.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayerchoiceResponseRewardCurrency)
export class PlayerchoiceResponseRewardCurrencyResolverBase {
  constructor(
    protected readonly service: PlayerchoiceResponseRewardCurrencyService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseRewardCurrency",
    action: "read",
    possession: "any",
  })
  async _playerchoiceResponseRewardCurrenciesMeta(
    @graphql.Args() args: PlayerchoiceResponseRewardCurrencyCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayerchoiceResponseRewardCurrency])
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseRewardCurrency",
    action: "read",
    possession: "any",
  })
  async playerchoiceResponseRewardCurrencies(
    @graphql.Args() args: PlayerchoiceResponseRewardCurrencyFindManyArgs
  ): Promise<PlayerchoiceResponseRewardCurrency[]> {
    return this.service.playerchoiceResponseRewardCurrencies(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayerchoiceResponseRewardCurrency, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseRewardCurrency",
    action: "read",
    possession: "own",
  })
  async playerchoiceResponseRewardCurrency(
    @graphql.Args() args: PlayerchoiceResponseRewardCurrencyFindUniqueArgs
  ): Promise<PlayerchoiceResponseRewardCurrency | null> {
    const result = await this.service.playerchoiceResponseRewardCurrency(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayerchoiceResponseRewardCurrency)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseRewardCurrency",
    action: "delete",
    possession: "any",
  })
  async deletePlayerchoiceResponseRewardCurrency(
    @graphql.Args() args: DeletePlayerchoiceResponseRewardCurrencyArgs
  ): Promise<PlayerchoiceResponseRewardCurrency | null> {
    try {
      return await this.service.deletePlayerchoiceResponseRewardCurrency(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
