/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { GameObjectQuestItem } from "./GameObjectQuestItem";
import { GameObjectQuestItemCountArgs } from "./GameObjectQuestItemCountArgs";
import { GameObjectQuestItemFindManyArgs } from "./GameObjectQuestItemFindManyArgs";
import { GameObjectQuestItemFindUniqueArgs } from "./GameObjectQuestItemFindUniqueArgs";
import { DeleteGameObjectQuestItemArgs } from "./DeleteGameObjectQuestItemArgs";
import { GameObjectQuestItemService } from "../gameObjectQuestItem.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => GameObjectQuestItem)
export class GameObjectQuestItemResolverBase {
  constructor(
    protected readonly service: GameObjectQuestItemService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "GameObjectQuestItem",
    action: "read",
    possession: "any",
  })
  async _gameObjectQuestItemsMeta(
    @graphql.Args() args: GameObjectQuestItemCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [GameObjectQuestItem])
  @nestAccessControl.UseRoles({
    resource: "GameObjectQuestItem",
    action: "read",
    possession: "any",
  })
  async gameObjectQuestItems(
    @graphql.Args() args: GameObjectQuestItemFindManyArgs
  ): Promise<GameObjectQuestItem[]> {
    return this.service.gameObjectQuestItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => GameObjectQuestItem, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "GameObjectQuestItem",
    action: "read",
    possession: "own",
  })
  async gameObjectQuestItem(
    @graphql.Args() args: GameObjectQuestItemFindUniqueArgs
  ): Promise<GameObjectQuestItem | null> {
    const result = await this.service.gameObjectQuestItem(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => GameObjectQuestItem)
  @nestAccessControl.UseRoles({
    resource: "GameObjectQuestItem",
    action: "delete",
    possession: "any",
  })
  async deleteGameObjectQuestItem(
    @graphql.Args() args: DeleteGameObjectQuestItemArgs
  ): Promise<GameObjectQuestItem | null> {
    try {
      return await this.service.deleteGameObjectQuestItem(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
