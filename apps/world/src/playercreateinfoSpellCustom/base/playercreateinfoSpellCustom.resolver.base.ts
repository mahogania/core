/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayercreateinfoSpellCustom } from "./PlayercreateinfoSpellCustom";
import { PlayercreateinfoSpellCustomCountArgs } from "./PlayercreateinfoSpellCustomCountArgs";
import { PlayercreateinfoSpellCustomFindManyArgs } from "./PlayercreateinfoSpellCustomFindManyArgs";
import { PlayercreateinfoSpellCustomFindUniqueArgs } from "./PlayercreateinfoSpellCustomFindUniqueArgs";
import { DeletePlayercreateinfoSpellCustomArgs } from "./DeletePlayercreateinfoSpellCustomArgs";
import { PlayercreateinfoSpellCustomService } from "../playercreateinfoSpellCustom.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayercreateinfoSpellCustom)
export class PlayercreateinfoSpellCustomResolverBase {
  constructor(
    protected readonly service: PlayercreateinfoSpellCustomService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoSpellCustom",
    action: "read",
    possession: "any",
  })
  async _playercreateinfoSpellCustomsMeta(
    @graphql.Args() args: PlayercreateinfoSpellCustomCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayercreateinfoSpellCustom])
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoSpellCustom",
    action: "read",
    possession: "any",
  })
  async playercreateinfoSpellCustoms(
    @graphql.Args() args: PlayercreateinfoSpellCustomFindManyArgs
  ): Promise<PlayercreateinfoSpellCustom[]> {
    return this.service.playercreateinfoSpellCustoms(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayercreateinfoSpellCustom, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoSpellCustom",
    action: "read",
    possession: "own",
  })
  async playercreateinfoSpellCustom(
    @graphql.Args() args: PlayercreateinfoSpellCustomFindUniqueArgs
  ): Promise<PlayercreateinfoSpellCustom | null> {
    const result = await this.service.playercreateinfoSpellCustom(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayercreateinfoSpellCustom)
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoSpellCustom",
    action: "delete",
    possession: "any",
  })
  async deletePlayercreateinfoSpellCustom(
    @graphql.Args() args: DeletePlayercreateinfoSpellCustomArgs
  ): Promise<PlayercreateinfoSpellCustom | null> {
    try {
      return await this.service.deletePlayercreateinfoSpellCustom(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
