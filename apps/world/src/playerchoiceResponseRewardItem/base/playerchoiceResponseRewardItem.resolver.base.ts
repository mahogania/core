/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayerchoiceResponseRewardItem } from "./PlayerchoiceResponseRewardItem";
import { PlayerchoiceResponseRewardItemCountArgs } from "./PlayerchoiceResponseRewardItemCountArgs";
import { PlayerchoiceResponseRewardItemFindManyArgs } from "./PlayerchoiceResponseRewardItemFindManyArgs";
import { PlayerchoiceResponseRewardItemFindUniqueArgs } from "./PlayerchoiceResponseRewardItemFindUniqueArgs";
import { DeletePlayerchoiceResponseRewardItemArgs } from "./DeletePlayerchoiceResponseRewardItemArgs";
import { PlayerchoiceResponseRewardItemService } from "../playerchoiceResponseRewardItem.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayerchoiceResponseRewardItem)
export class PlayerchoiceResponseRewardItemResolverBase {
  constructor(
    protected readonly service: PlayerchoiceResponseRewardItemService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseRewardItem",
    action: "read",
    possession: "any",
  })
  async _playerchoiceResponseRewardItemsMeta(
    @graphql.Args() args: PlayerchoiceResponseRewardItemCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayerchoiceResponseRewardItem])
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseRewardItem",
    action: "read",
    possession: "any",
  })
  async playerchoiceResponseRewardItems(
    @graphql.Args() args: PlayerchoiceResponseRewardItemFindManyArgs
  ): Promise<PlayerchoiceResponseRewardItem[]> {
    return this.service.playerchoiceResponseRewardItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayerchoiceResponseRewardItem, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseRewardItem",
    action: "read",
    possession: "own",
  })
  async playerchoiceResponseRewardItem(
    @graphql.Args() args: PlayerchoiceResponseRewardItemFindUniqueArgs
  ): Promise<PlayerchoiceResponseRewardItem | null> {
    const result = await this.service.playerchoiceResponseRewardItem(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayerchoiceResponseRewardItem)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseRewardItem",
    action: "delete",
    possession: "any",
  })
  async deletePlayerchoiceResponseRewardItem(
    @graphql.Args() args: DeletePlayerchoiceResponseRewardItemArgs
  ): Promise<PlayerchoiceResponseRewardItem | null> {
    try {
      return await this.service.deletePlayerchoiceResponseRewardItem(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
