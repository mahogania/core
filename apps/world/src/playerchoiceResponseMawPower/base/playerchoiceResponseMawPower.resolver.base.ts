/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayerchoiceResponseMawPower } from "./PlayerchoiceResponseMawPower";
import { PlayerchoiceResponseMawPowerCountArgs } from "./PlayerchoiceResponseMawPowerCountArgs";
import { PlayerchoiceResponseMawPowerFindManyArgs } from "./PlayerchoiceResponseMawPowerFindManyArgs";
import { PlayerchoiceResponseMawPowerFindUniqueArgs } from "./PlayerchoiceResponseMawPowerFindUniqueArgs";
import { DeletePlayerchoiceResponseMawPowerArgs } from "./DeletePlayerchoiceResponseMawPowerArgs";
import { PlayerchoiceResponseMawPowerService } from "../playerchoiceResponseMawPower.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayerchoiceResponseMawPower)
export class PlayerchoiceResponseMawPowerResolverBase {
  constructor(
    protected readonly service: PlayerchoiceResponseMawPowerService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseMawPower",
    action: "read",
    possession: "any",
  })
  async _playerchoiceResponseMawPowersMeta(
    @graphql.Args() args: PlayerchoiceResponseMawPowerCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayerchoiceResponseMawPower])
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseMawPower",
    action: "read",
    possession: "any",
  })
  async playerchoiceResponseMawPowers(
    @graphql.Args() args: PlayerchoiceResponseMawPowerFindManyArgs
  ): Promise<PlayerchoiceResponseMawPower[]> {
    return this.service.playerchoiceResponseMawPowers(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayerchoiceResponseMawPower, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseMawPower",
    action: "read",
    possession: "own",
  })
  async playerchoiceResponseMawPower(
    @graphql.Args() args: PlayerchoiceResponseMawPowerFindUniqueArgs
  ): Promise<PlayerchoiceResponseMawPower | null> {
    const result = await this.service.playerchoiceResponseMawPower(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayerchoiceResponseMawPower)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponseMawPower",
    action: "delete",
    possession: "any",
  })
  async deletePlayerchoiceResponseMawPower(
    @graphql.Args() args: DeletePlayerchoiceResponseMawPowerArgs
  ): Promise<PlayerchoiceResponseMawPower | null> {
    try {
      return await this.service.deletePlayerchoiceResponseMawPower(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
