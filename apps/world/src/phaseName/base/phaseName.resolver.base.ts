/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PhaseName } from "./PhaseName";
import { PhaseNameCountArgs } from "./PhaseNameCountArgs";
import { PhaseNameFindManyArgs } from "./PhaseNameFindManyArgs";
import { PhaseNameFindUniqueArgs } from "./PhaseNameFindUniqueArgs";
import { DeletePhaseNameArgs } from "./DeletePhaseNameArgs";
import { PhaseNameService } from "../phaseName.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PhaseName)
export class PhaseNameResolverBase {
  constructor(
    protected readonly service: PhaseNameService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PhaseName",
    action: "read",
    possession: "any",
  })
  async _phaseNamesMeta(
    @graphql.Args() args: PhaseNameCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PhaseName])
  @nestAccessControl.UseRoles({
    resource: "PhaseName",
    action: "read",
    possession: "any",
  })
  async phaseNames(
    @graphql.Args() args: PhaseNameFindManyArgs
  ): Promise<PhaseName[]> {
    return this.service.phaseNames(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PhaseName, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PhaseName",
    action: "read",
    possession: "own",
  })
  async phaseName(
    @graphql.Args() args: PhaseNameFindUniqueArgs
  ): Promise<PhaseName | null> {
    const result = await this.service.phaseName(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PhaseName)
  @nestAccessControl.UseRoles({
    resource: "PhaseName",
    action: "delete",
    possession: "any",
  })
  async deletePhaseName(
    @graphql.Args() args: DeletePhaseNameArgs
  ): Promise<PhaseName | null> {
    try {
      return await this.service.deletePhaseName(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
