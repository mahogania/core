/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayerchoiceResponse } from "./PlayerchoiceResponse";
import { PlayerchoiceResponseCountArgs } from "./PlayerchoiceResponseCountArgs";
import { PlayerchoiceResponseFindManyArgs } from "./PlayerchoiceResponseFindManyArgs";
import { PlayerchoiceResponseFindUniqueArgs } from "./PlayerchoiceResponseFindUniqueArgs";
import { DeletePlayerchoiceResponseArgs } from "./DeletePlayerchoiceResponseArgs";
import { PlayerchoiceResponseService } from "../playerchoiceResponse.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayerchoiceResponse)
export class PlayerchoiceResponseResolverBase {
  constructor(
    protected readonly service: PlayerchoiceResponseService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponse",
    action: "read",
    possession: "any",
  })
  async _playerchoiceResponsesMeta(
    @graphql.Args() args: PlayerchoiceResponseCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayerchoiceResponse])
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponse",
    action: "read",
    possession: "any",
  })
  async playerchoiceResponses(
    @graphql.Args() args: PlayerchoiceResponseFindManyArgs
  ): Promise<PlayerchoiceResponse[]> {
    return this.service.playerchoiceResponses(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayerchoiceResponse, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponse",
    action: "read",
    possession: "own",
  })
  async playerchoiceResponse(
    @graphql.Args() args: PlayerchoiceResponseFindUniqueArgs
  ): Promise<PlayerchoiceResponse | null> {
    const result = await this.service.playerchoiceResponse(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayerchoiceResponse)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceResponse",
    action: "delete",
    possession: "any",
  })
  async deletePlayerchoiceResponse(
    @graphql.Args() args: DeletePlayerchoiceResponseArgs
  ): Promise<PlayerchoiceResponse | null> {
    try {
      return await this.service.deletePlayerchoiceResponse(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
