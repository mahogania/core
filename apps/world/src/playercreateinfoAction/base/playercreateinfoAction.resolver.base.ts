/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayercreateinfoAction } from "./PlayercreateinfoAction";
import { PlayercreateinfoActionCountArgs } from "./PlayercreateinfoActionCountArgs";
import { PlayercreateinfoActionFindManyArgs } from "./PlayercreateinfoActionFindManyArgs";
import { PlayercreateinfoActionFindUniqueArgs } from "./PlayercreateinfoActionFindUniqueArgs";
import { DeletePlayercreateinfoActionArgs } from "./DeletePlayercreateinfoActionArgs";
import { PlayercreateinfoActionService } from "../playercreateinfoAction.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayercreateinfoAction)
export class PlayercreateinfoActionResolverBase {
  constructor(
    protected readonly service: PlayercreateinfoActionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoAction",
    action: "read",
    possession: "any",
  })
  async _playercreateinfoActionsMeta(
    @graphql.Args() args: PlayercreateinfoActionCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayercreateinfoAction])
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoAction",
    action: "read",
    possession: "any",
  })
  async playercreateinfoActions(
    @graphql.Args() args: PlayercreateinfoActionFindManyArgs
  ): Promise<PlayercreateinfoAction[]> {
    return this.service.playercreateinfoActions(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayercreateinfoAction, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoAction",
    action: "read",
    possession: "own",
  })
  async playercreateinfoAction(
    @graphql.Args() args: PlayercreateinfoActionFindUniqueArgs
  ): Promise<PlayercreateinfoAction | null> {
    const result = await this.service.playercreateinfoAction(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayercreateinfoAction)
  @nestAccessControl.UseRoles({
    resource: "PlayercreateinfoAction",
    action: "delete",
    possession: "any",
  })
  async deletePlayercreateinfoAction(
    @graphql.Args() args: DeletePlayercreateinfoActionArgs
  ): Promise<PlayercreateinfoAction | null> {
    try {
      return await this.service.deletePlayercreateinfoAction(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
