/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { InstanceSpawnGroups } from "./InstanceSpawnGroups";
import { InstanceSpawnGroupsCountArgs } from "./InstanceSpawnGroupsCountArgs";
import { InstanceSpawnGroupsFindManyArgs } from "./InstanceSpawnGroupsFindManyArgs";
import { InstanceSpawnGroupsFindUniqueArgs } from "./InstanceSpawnGroupsFindUniqueArgs";
import { DeleteInstanceSpawnGroupsArgs } from "./DeleteInstanceSpawnGroupsArgs";
import { InstanceSpawnGroupsService } from "../instanceSpawnGroups.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => InstanceSpawnGroups)
export class InstanceSpawnGroupsResolverBase {
  constructor(
    protected readonly service: InstanceSpawnGroupsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "InstanceSpawnGroups",
    action: "read",
    possession: "any",
  })
  async _instanceSpawnGroupsItemsMeta(
    @graphql.Args() args: InstanceSpawnGroupsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [InstanceSpawnGroups])
  @nestAccessControl.UseRoles({
    resource: "InstanceSpawnGroups",
    action: "read",
    possession: "any",
  })
  async instanceSpawnGroupsItems(
    @graphql.Args() args: InstanceSpawnGroupsFindManyArgs
  ): Promise<InstanceSpawnGroups[]> {
    return this.service.instanceSpawnGroupsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => InstanceSpawnGroups, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "InstanceSpawnGroups",
    action: "read",
    possession: "own",
  })
  async instanceSpawnGroups(
    @graphql.Args() args: InstanceSpawnGroupsFindUniqueArgs
  ): Promise<InstanceSpawnGroups | null> {
    const result = await this.service.instanceSpawnGroups(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => InstanceSpawnGroups)
  @nestAccessControl.UseRoles({
    resource: "InstanceSpawnGroups",
    action: "delete",
    possession: "any",
  })
  async deleteInstanceSpawnGroups(
    @graphql.Args() args: DeleteInstanceSpawnGroupsArgs
  ): Promise<InstanceSpawnGroups | null> {
    try {
      return await this.service.deleteInstanceSpawnGroups(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
