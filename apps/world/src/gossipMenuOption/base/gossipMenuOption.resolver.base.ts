/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { GossipMenuOption } from "./GossipMenuOption";
import { GossipMenuOptionCountArgs } from "./GossipMenuOptionCountArgs";
import { GossipMenuOptionFindManyArgs } from "./GossipMenuOptionFindManyArgs";
import { GossipMenuOptionFindUniqueArgs } from "./GossipMenuOptionFindUniqueArgs";
import { DeleteGossipMenuOptionArgs } from "./DeleteGossipMenuOptionArgs";
import { GossipMenuOptionService } from "../gossipMenuOption.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => GossipMenuOption)
export class GossipMenuOptionResolverBase {
  constructor(
    protected readonly service: GossipMenuOptionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "GossipMenuOption",
    action: "read",
    possession: "any",
  })
  async _gossipMenuOptionsMeta(
    @graphql.Args() args: GossipMenuOptionCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [GossipMenuOption])
  @nestAccessControl.UseRoles({
    resource: "GossipMenuOption",
    action: "read",
    possession: "any",
  })
  async gossipMenuOptions(
    @graphql.Args() args: GossipMenuOptionFindManyArgs
  ): Promise<GossipMenuOption[]> {
    return this.service.gossipMenuOptions(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => GossipMenuOption, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "GossipMenuOption",
    action: "read",
    possession: "own",
  })
  async gossipMenuOption(
    @graphql.Args() args: GossipMenuOptionFindUniqueArgs
  ): Promise<GossipMenuOption | null> {
    const result = await this.service.gossipMenuOption(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => GossipMenuOption)
  @nestAccessControl.UseRoles({
    resource: "GossipMenuOption",
    action: "delete",
    possession: "any",
  })
  async deleteGossipMenuOption(
    @graphql.Args() args: DeleteGossipMenuOptionArgs
  ): Promise<GossipMenuOption | null> {
    try {
      return await this.service.deleteGossipMenuOption(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
