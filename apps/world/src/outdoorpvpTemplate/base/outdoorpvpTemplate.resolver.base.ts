/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { OutdoorpvpTemplate } from "./OutdoorpvpTemplate";
import { OutdoorpvpTemplateCountArgs } from "./OutdoorpvpTemplateCountArgs";
import { OutdoorpvpTemplateFindManyArgs } from "./OutdoorpvpTemplateFindManyArgs";
import { OutdoorpvpTemplateFindUniqueArgs } from "./OutdoorpvpTemplateFindUniqueArgs";
import { DeleteOutdoorpvpTemplateArgs } from "./DeleteOutdoorpvpTemplateArgs";
import { OutdoorpvpTemplateService } from "../outdoorpvpTemplate.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => OutdoorpvpTemplate)
export class OutdoorpvpTemplateResolverBase {
  constructor(
    protected readonly service: OutdoorpvpTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "OutdoorpvpTemplate",
    action: "read",
    possession: "any",
  })
  async _outdoorpvpTemplatesMeta(
    @graphql.Args() args: OutdoorpvpTemplateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [OutdoorpvpTemplate])
  @nestAccessControl.UseRoles({
    resource: "OutdoorpvpTemplate",
    action: "read",
    possession: "any",
  })
  async outdoorpvpTemplates(
    @graphql.Args() args: OutdoorpvpTemplateFindManyArgs
  ): Promise<OutdoorpvpTemplate[]> {
    return this.service.outdoorpvpTemplates(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => OutdoorpvpTemplate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "OutdoorpvpTemplate",
    action: "read",
    possession: "own",
  })
  async outdoorpvpTemplate(
    @graphql.Args() args: OutdoorpvpTemplateFindUniqueArgs
  ): Promise<OutdoorpvpTemplate | null> {
    const result = await this.service.outdoorpvpTemplate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => OutdoorpvpTemplate)
  @nestAccessControl.UseRoles({
    resource: "OutdoorpvpTemplate",
    action: "delete",
    possession: "any",
  })
  async deleteOutdoorpvpTemplate(
    @graphql.Args() args: DeleteOutdoorpvpTemplateArgs
  ): Promise<OutdoorpvpTemplate | null> {
    try {
      return await this.service.deleteOutdoorpvpTemplate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
