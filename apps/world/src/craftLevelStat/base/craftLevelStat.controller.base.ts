/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { CraftLevelStatService } from "../craftLevelStat.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { CraftLevelStatCreateInput } from "./CraftLevelStatCreateInput";
import { CraftLevelStat } from "./CraftLevelStat";
import { CraftLevelStatFindManyArgs } from "./CraftLevelStatFindManyArgs";
import { CraftLevelStatWhereUniqueInput } from "./CraftLevelStatWhereUniqueInput";
import { CraftLevelStatUpdateInput } from "./CraftLevelStatUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class CraftLevelStatControllerBase {
  constructor(
    protected readonly service: CraftLevelStatService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: CraftLevelStat })
  @nestAccessControl.UseRoles({
    resource: "CraftLevelStat",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: CraftLevelStatCreateInput,
  })
  async createCraftLevelStat(
    @common.Body() data: CraftLevelStatCreateInput
  ): Promise<CraftLevelStat> {
    return await this.service.createCraftLevelStat({
      data: {
        ...data,

        craft: {
          connect: data.craft,
        },
      },
      select: {
        craft: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        level: true,
        stat: true,
        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [CraftLevelStat] })
  @ApiNestedQuery(CraftLevelStatFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "CraftLevelStat",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async craftLevelStats(
    @common.Req() request: Request
  ): Promise<CraftLevelStat[]> {
    const args = plainToClass(CraftLevelStatFindManyArgs, request.query);
    return this.service.craftLevelStats({
      ...args,
      select: {
        craft: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        level: true,
        stat: true,
        updatedAt: true,
        value: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: CraftLevelStat })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CraftLevelStat",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async craftLevelStat(
    @common.Param() params: CraftLevelStatWhereUniqueInput
  ): Promise<CraftLevelStat | null> {
    const result = await this.service.craftLevelStat({
      where: params,
      select: {
        craft: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        level: true,
        stat: true,
        updatedAt: true,
        value: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: CraftLevelStat })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CraftLevelStat",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: CraftLevelStatUpdateInput,
  })
  async updateCraftLevelStat(
    @common.Param() params: CraftLevelStatWhereUniqueInput,
    @common.Body() data: CraftLevelStatUpdateInput
  ): Promise<CraftLevelStat | null> {
    try {
      return await this.service.updateCraftLevelStat({
        where: params,
        data: {
          ...data,

          craft: {
            connect: data.craft,
          },
        },
        select: {
          craft: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          level: true,
          stat: true,
          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: CraftLevelStat })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "CraftLevelStat",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteCraftLevelStat(
    @common.Param() params: CraftLevelStatWhereUniqueInput
  ): Promise<CraftLevelStat | null> {
    try {
      return await this.service.deleteCraftLevelStat({
        where: params,
        select: {
          craft: {
            select: {
              id: true,
            },
          },

          createdAt: true,
          id: true,
          level: true,
          stat: true,
          updatedAt: true,
          value: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
