/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { GrpcMethod } from "@nestjs/microservices";
import { ZoneService } from "../zone.service";
import { ZoneCreateInput } from "./ZoneCreateInput";
import { ZoneWhereInput } from "./ZoneWhereInput";
import { ZoneWhereUniqueInput } from "./ZoneWhereUniqueInput";
import { ZoneFindManyArgs } from "./ZoneFindManyArgs";
import { ZoneUpdateInput } from "./ZoneUpdateInput";
import { Zone } from "./Zone";

export class ZoneGrpcControllerBase {
  constructor(protected readonly service: ZoneService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Zone })
  @GrpcMethod("ZoneService", "createZone")
  async createZone(@common.Body() data: ZoneCreateInput): Promise<Zone> {
    return await this.service.createZone({
      data: data,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Zone] })
  @ApiNestedQuery(ZoneFindManyArgs)
  @GrpcMethod("ZoneService", "zones")
  async zones(@common.Req() request: Request): Promise<Zone[]> {
    const args = plainToClass(ZoneFindManyArgs, request.query);
    return this.service.zones({
      ...args,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Zone })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("ZoneService", "zone")
  async zone(
    @common.Param() params: ZoneWhereUniqueInput
  ): Promise<Zone | null> {
    const result = await this.service.zone({
      where: params,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Zone })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("ZoneService", "updateZone")
  async updateZone(
    @common.Param() params: ZoneWhereUniqueInput,
    @common.Body() data: ZoneUpdateInput
  ): Promise<Zone | null> {
    try {
      return await this.service.updateZone({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Zone })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("ZoneService", "deleteZone")
  async deleteZone(
    @common.Param() params: ZoneWhereUniqueInput
  ): Promise<Zone | null> {
    try {
      return await this.service.deleteZone({
        where: params,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
