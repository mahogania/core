/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayerchoiceLocale } from "./PlayerchoiceLocale";
import { PlayerchoiceLocaleCountArgs } from "./PlayerchoiceLocaleCountArgs";
import { PlayerchoiceLocaleFindManyArgs } from "./PlayerchoiceLocaleFindManyArgs";
import { PlayerchoiceLocaleFindUniqueArgs } from "./PlayerchoiceLocaleFindUniqueArgs";
import { DeletePlayerchoiceLocaleArgs } from "./DeletePlayerchoiceLocaleArgs";
import { PlayerchoiceLocaleService } from "../playerchoiceLocale.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayerchoiceLocale)
export class PlayerchoiceLocaleResolverBase {
  constructor(
    protected readonly service: PlayerchoiceLocaleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceLocale",
    action: "read",
    possession: "any",
  })
  async _playerchoiceLocalesMeta(
    @graphql.Args() args: PlayerchoiceLocaleCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayerchoiceLocale])
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceLocale",
    action: "read",
    possession: "any",
  })
  async playerchoiceLocales(
    @graphql.Args() args: PlayerchoiceLocaleFindManyArgs
  ): Promise<PlayerchoiceLocale[]> {
    return this.service.playerchoiceLocales(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayerchoiceLocale, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceLocale",
    action: "read",
    possession: "own",
  })
  async playerchoiceLocale(
    @graphql.Args() args: PlayerchoiceLocaleFindUniqueArgs
  ): Promise<PlayerchoiceLocale | null> {
    const result = await this.service.playerchoiceLocale(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayerchoiceLocale)
  @nestAccessControl.UseRoles({
    resource: "PlayerchoiceLocale",
    action: "delete",
    possession: "any",
  })
  async deletePlayerchoiceLocale(
    @graphql.Args() args: DeletePlayerchoiceLocaleArgs
  ): Promise<PlayerchoiceLocale | null> {
    try {
      return await this.service.deletePlayerchoiceLocale(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
