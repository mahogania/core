/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { MountDefinitions } from "./MountDefinitions";
import { MountDefinitionsCountArgs } from "./MountDefinitionsCountArgs";
import { MountDefinitionsFindManyArgs } from "./MountDefinitionsFindManyArgs";
import { MountDefinitionsFindUniqueArgs } from "./MountDefinitionsFindUniqueArgs";
import { DeleteMountDefinitionsArgs } from "./DeleteMountDefinitionsArgs";
import { MountDefinitionsService } from "../mountDefinitions.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => MountDefinitions)
export class MountDefinitionsResolverBase {
  constructor(
    protected readonly service: MountDefinitionsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "MountDefinitions",
    action: "read",
    possession: "any",
  })
  async _mountDefinitionsItemsMeta(
    @graphql.Args() args: MountDefinitionsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [MountDefinitions])
  @nestAccessControl.UseRoles({
    resource: "MountDefinitions",
    action: "read",
    possession: "any",
  })
  async mountDefinitionsItems(
    @graphql.Args() args: MountDefinitionsFindManyArgs
  ): Promise<MountDefinitions[]> {
    return this.service.mountDefinitionsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => MountDefinitions, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "MountDefinitions",
    action: "read",
    possession: "own",
  })
  async mountDefinitions(
    @graphql.Args() args: MountDefinitionsFindUniqueArgs
  ): Promise<MountDefinitions | null> {
    const result = await this.service.mountDefinitions(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => MountDefinitions)
  @nestAccessControl.UseRoles({
    resource: "MountDefinitions",
    action: "delete",
    possession: "any",
  })
  async deleteMountDefinitions(
    @graphql.Args() args: DeleteMountDefinitionsArgs
  ): Promise<MountDefinitions | null> {
    try {
      return await this.service.deleteMountDefinitions(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
