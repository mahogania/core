/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PlayerReward } from "./PlayerReward";
import { PlayerRewardCountArgs } from "./PlayerRewardCountArgs";
import { PlayerRewardFindManyArgs } from "./PlayerRewardFindManyArgs";
import { PlayerRewardFindUniqueArgs } from "./PlayerRewardFindUniqueArgs";
import { DeletePlayerRewardArgs } from "./DeletePlayerRewardArgs";
import { PlayerRewardService } from "../playerReward.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PlayerReward)
export class PlayerRewardResolverBase {
  constructor(
    protected readonly service: PlayerRewardService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PlayerReward",
    action: "read",
    possession: "any",
  })
  async _playerRewardsMeta(
    @graphql.Args() args: PlayerRewardCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PlayerReward])
  @nestAccessControl.UseRoles({
    resource: "PlayerReward",
    action: "read",
    possession: "any",
  })
  async playerRewards(
    @graphql.Args() args: PlayerRewardFindManyArgs
  ): Promise<PlayerReward[]> {
    return this.service.playerRewards(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PlayerReward, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PlayerReward",
    action: "read",
    possession: "own",
  })
  async playerReward(
    @graphql.Args() args: PlayerRewardFindUniqueArgs
  ): Promise<PlayerReward | null> {
    const result = await this.service.playerReward(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PlayerReward)
  @nestAccessControl.UseRoles({
    resource: "PlayerReward",
    action: "delete",
    possession: "any",
  })
  async deletePlayerReward(
    @graphql.Args() args: DeletePlayerRewardArgs
  ): Promise<PlayerReward | null> {
    try {
      return await this.service.deletePlayerReward(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
