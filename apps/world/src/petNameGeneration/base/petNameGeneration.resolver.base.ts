/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PetNameGeneration } from "./PetNameGeneration";
import { PetNameGenerationCountArgs } from "./PetNameGenerationCountArgs";
import { PetNameGenerationFindManyArgs } from "./PetNameGenerationFindManyArgs";
import { PetNameGenerationFindUniqueArgs } from "./PetNameGenerationFindUniqueArgs";
import { DeletePetNameGenerationArgs } from "./DeletePetNameGenerationArgs";
import { PetNameGenerationService } from "../petNameGeneration.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PetNameGeneration)
export class PetNameGenerationResolverBase {
  constructor(
    protected readonly service: PetNameGenerationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PetNameGeneration",
    action: "read",
    possession: "any",
  })
  async _petNameGenerationsMeta(
    @graphql.Args() args: PetNameGenerationCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PetNameGeneration])
  @nestAccessControl.UseRoles({
    resource: "PetNameGeneration",
    action: "read",
    possession: "any",
  })
  async petNameGenerations(
    @graphql.Args() args: PetNameGenerationFindManyArgs
  ): Promise<PetNameGeneration[]> {
    return this.service.petNameGenerations(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PetNameGeneration, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PetNameGeneration",
    action: "read",
    possession: "own",
  })
  async petNameGeneration(
    @graphql.Args() args: PetNameGenerationFindUniqueArgs
  ): Promise<PetNameGeneration | null> {
    const result = await this.service.petNameGeneration(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PetNameGeneration)
  @nestAccessControl.UseRoles({
    resource: "PetNameGeneration",
    action: "delete",
    possession: "any",
  })
  async deletePetNameGeneration(
    @graphql.Args() args: DeletePetNameGenerationArgs
  ): Promise<PetNameGeneration | null> {
    try {
      return await this.service.deletePetNameGeneration(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
