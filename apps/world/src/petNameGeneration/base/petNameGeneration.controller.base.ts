/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { PetNameGenerationService } from "../petNameGeneration.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PetNameGenerationCreateInput } from "./PetNameGenerationCreateInput";
import { PetNameGeneration } from "./PetNameGeneration";
import { PetNameGenerationFindManyArgs } from "./PetNameGenerationFindManyArgs";
import { PetNameGenerationWhereUniqueInput } from "./PetNameGenerationWhereUniqueInput";
import { PetNameGenerationUpdateInput } from "./PetNameGenerationUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class PetNameGenerationControllerBase {
  constructor(
    protected readonly service: PetNameGenerationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PetNameGeneration })
  @nestAccessControl.UseRoles({
    resource: "PetNameGeneration",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: PetNameGenerationCreateInput,
  })
  async createPetNameGeneration(
    @common.Body() data: PetNameGenerationCreateInput
  ): Promise<PetNameGeneration> {
    return await this.service.createPetNameGeneration({
      data: data,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [PetNameGeneration] })
  @ApiNestedQuery(PetNameGenerationFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "PetNameGeneration",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async petNameGenerations(
    @common.Req() request: Request
  ): Promise<PetNameGeneration[]> {
    const args = plainToClass(PetNameGenerationFindManyArgs, request.query);
    return this.service.petNameGenerations({
      ...args,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PetNameGeneration })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PetNameGeneration",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async petNameGeneration(
    @common.Param() params: PetNameGenerationWhereUniqueInput
  ): Promise<PetNameGeneration | null> {
    const result = await this.service.petNameGeneration({
      where: params,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PetNameGeneration })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PetNameGeneration",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: PetNameGenerationUpdateInput,
  })
  async updatePetNameGeneration(
    @common.Param() params: PetNameGenerationWhereUniqueInput,
    @common.Body() data: PetNameGenerationUpdateInput
  ): Promise<PetNameGeneration | null> {
    try {
      return await this.service.updatePetNameGeneration({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PetNameGeneration })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "PetNameGeneration",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deletePetNameGeneration(
    @common.Param() params: PetNameGenerationWhereUniqueInput
  ): Promise<PetNameGeneration | null> {
    try {
      return await this.service.deletePetNameGeneration({
        where: params,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
