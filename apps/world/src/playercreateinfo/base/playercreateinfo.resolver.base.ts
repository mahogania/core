/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { Playercreateinfo } from "./Playercreateinfo";
import { PlayercreateinfoCountArgs } from "./PlayercreateinfoCountArgs";
import { PlayercreateinfoFindManyArgs } from "./PlayercreateinfoFindManyArgs";
import { PlayercreateinfoFindUniqueArgs } from "./PlayercreateinfoFindUniqueArgs";
import { DeletePlayercreateinfoArgs } from "./DeletePlayercreateinfoArgs";
import { PlayercreateinfoService } from "../playercreateinfo.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Playercreateinfo)
export class PlayercreateinfoResolverBase {
  constructor(
    protected readonly service: PlayercreateinfoService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Playercreateinfo",
    action: "read",
    possession: "any",
  })
  async _playercreateinfosMeta(
    @graphql.Args() args: PlayercreateinfoCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Playercreateinfo])
  @nestAccessControl.UseRoles({
    resource: "Playercreateinfo",
    action: "read",
    possession: "any",
  })
  async playercreateinfos(
    @graphql.Args() args: PlayercreateinfoFindManyArgs
  ): Promise<Playercreateinfo[]> {
    return this.service.playercreateinfos(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Playercreateinfo, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Playercreateinfo",
    action: "read",
    possession: "own",
  })
  async playercreateinfo(
    @graphql.Args() args: PlayercreateinfoFindUniqueArgs
  ): Promise<Playercreateinfo | null> {
    const result = await this.service.playercreateinfo(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Playercreateinfo)
  @nestAccessControl.UseRoles({
    resource: "Playercreateinfo",
    action: "delete",
    possession: "any",
  })
  async deletePlayercreateinfo(
    @graphql.Args() args: DeletePlayercreateinfoArgs
  ): Promise<Playercreateinfo | null> {
    try {
      return await this.service.deletePlayercreateinfo(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
