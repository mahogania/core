/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { PhaseArea } from "./PhaseArea";
import { PhaseAreaCountArgs } from "./PhaseAreaCountArgs";
import { PhaseAreaFindManyArgs } from "./PhaseAreaFindManyArgs";
import { PhaseAreaFindUniqueArgs } from "./PhaseAreaFindUniqueArgs";
import { DeletePhaseAreaArgs } from "./DeletePhaseAreaArgs";
import { PhaseAreaService } from "../phaseArea.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => PhaseArea)
export class PhaseAreaResolverBase {
  constructor(
    protected readonly service: PhaseAreaService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "PhaseArea",
    action: "read",
    possession: "any",
  })
  async _phaseAreasMeta(
    @graphql.Args() args: PhaseAreaCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [PhaseArea])
  @nestAccessControl.UseRoles({
    resource: "PhaseArea",
    action: "read",
    possession: "any",
  })
  async phaseAreas(
    @graphql.Args() args: PhaseAreaFindManyArgs
  ): Promise<PhaseArea[]> {
    return this.service.phaseAreas(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => PhaseArea, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "PhaseArea",
    action: "read",
    possession: "own",
  })
  async phaseArea(
    @graphql.Args() args: PhaseAreaFindUniqueArgs
  ): Promise<PhaseArea | null> {
    const result = await this.service.phaseArea(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => PhaseArea)
  @nestAccessControl.UseRoles({
    resource: "PhaseArea",
    action: "delete",
    possession: "any",
  })
  async deletePhaseArea(
    @graphql.Args() args: DeletePhaseAreaArgs
  ): Promise<PhaseArea | null> {
    try {
      return await this.service.deletePhaseArea(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
