/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { LfgDungeonTemplate } from "./LfgDungeonTemplate";
import { LfgDungeonTemplateCountArgs } from "./LfgDungeonTemplateCountArgs";
import { LfgDungeonTemplateFindManyArgs } from "./LfgDungeonTemplateFindManyArgs";
import { LfgDungeonTemplateFindUniqueArgs } from "./LfgDungeonTemplateFindUniqueArgs";
import { DeleteLfgDungeonTemplateArgs } from "./DeleteLfgDungeonTemplateArgs";
import { LfgDungeonTemplateService } from "../lfgDungeonTemplate.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => LfgDungeonTemplate)
export class LfgDungeonTemplateResolverBase {
  constructor(
    protected readonly service: LfgDungeonTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "LfgDungeonTemplate",
    action: "read",
    possession: "any",
  })
  async _lfgDungeonTemplatesMeta(
    @graphql.Args() args: LfgDungeonTemplateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [LfgDungeonTemplate])
  @nestAccessControl.UseRoles({
    resource: "LfgDungeonTemplate",
    action: "read",
    possession: "any",
  })
  async lfgDungeonTemplates(
    @graphql.Args() args: LfgDungeonTemplateFindManyArgs
  ): Promise<LfgDungeonTemplate[]> {
    return this.service.lfgDungeonTemplates(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => LfgDungeonTemplate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "LfgDungeonTemplate",
    action: "read",
    possession: "own",
  })
  async lfgDungeonTemplate(
    @graphql.Args() args: LfgDungeonTemplateFindUniqueArgs
  ): Promise<LfgDungeonTemplate | null> {
    const result = await this.service.lfgDungeonTemplate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => LfgDungeonTemplate)
  @nestAccessControl.UseRoles({
    resource: "LfgDungeonTemplate",
    action: "delete",
    possession: "any",
  })
  async deleteLfgDungeonTemplate(
    @graphql.Args() args: DeleteLfgDungeonTemplateArgs
  ): Promise<LfgDungeonTemplate | null> {
    try {
      return await this.service.deleteLfgDungeonTemplate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
