/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { NpcText } from "./NpcText";
import { NpcTextCountArgs } from "./NpcTextCountArgs";
import { NpcTextFindManyArgs } from "./NpcTextFindManyArgs";
import { NpcTextFindUniqueArgs } from "./NpcTextFindUniqueArgs";
import { DeleteNpcTextArgs } from "./DeleteNpcTextArgs";
import { NpcTextService } from "../npcText.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => NpcText)
export class NpcTextResolverBase {
  constructor(
    protected readonly service: NpcTextService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "NpcText",
    action: "read",
    possession: "any",
  })
  async _npcTextsMeta(
    @graphql.Args() args: NpcTextCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [NpcText])
  @nestAccessControl.UseRoles({
    resource: "NpcText",
    action: "read",
    possession: "any",
  })
  async npcTexts(
    @graphql.Args() args: NpcTextFindManyArgs
  ): Promise<NpcText[]> {
    return this.service.npcTexts(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => NpcText, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "NpcText",
    action: "read",
    possession: "own",
  })
  async npcText(
    @graphql.Args() args: NpcTextFindUniqueArgs
  ): Promise<NpcText | null> {
    const result = await this.service.npcText(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => NpcText)
  @nestAccessControl.UseRoles({
    resource: "NpcText",
    action: "delete",
    possession: "any",
  })
  async deleteNpcText(
    @graphql.Args() args: DeleteNpcTextArgs
  ): Promise<NpcText | null> {
    try {
      return await this.service.deleteNpcText(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
