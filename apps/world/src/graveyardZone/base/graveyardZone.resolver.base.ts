/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { GraveyardZone } from "./GraveyardZone";
import { GraveyardZoneCountArgs } from "./GraveyardZoneCountArgs";
import { GraveyardZoneFindManyArgs } from "./GraveyardZoneFindManyArgs";
import { GraveyardZoneFindUniqueArgs } from "./GraveyardZoneFindUniqueArgs";
import { DeleteGraveyardZoneArgs } from "./DeleteGraveyardZoneArgs";
import { GraveyardZoneService } from "../graveyardZone.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => GraveyardZone)
export class GraveyardZoneResolverBase {
  constructor(
    protected readonly service: GraveyardZoneService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "GraveyardZone",
    action: "read",
    possession: "any",
  })
  async _graveyardZonesMeta(
    @graphql.Args() args: GraveyardZoneCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [GraveyardZone])
  @nestAccessControl.UseRoles({
    resource: "GraveyardZone",
    action: "read",
    possession: "any",
  })
  async graveyardZones(
    @graphql.Args() args: GraveyardZoneFindManyArgs
  ): Promise<GraveyardZone[]> {
    return this.service.graveyardZones(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => GraveyardZone, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "GraveyardZone",
    action: "read",
    possession: "own",
  })
  async graveyardZone(
    @graphql.Args() args: GraveyardZoneFindUniqueArgs
  ): Promise<GraveyardZone | null> {
    const result = await this.service.graveyardZone(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => GraveyardZone)
  @nestAccessControl.UseRoles({
    resource: "GraveyardZone",
    action: "delete",
    possession: "any",
  })
  async deleteGraveyardZone(
    @graphql.Args() args: DeleteGraveyardZoneArgs
  ): Promise<GraveyardZone | null> {
    try {
      return await this.service.deleteGraveyardZone(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
