/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { GameObjectQuestStarter } from "./GameObjectQuestStarter";
import { GameObjectQuestStarterCountArgs } from "./GameObjectQuestStarterCountArgs";
import { GameObjectQuestStarterFindManyArgs } from "./GameObjectQuestStarterFindManyArgs";
import { GameObjectQuestStarterFindUniqueArgs } from "./GameObjectQuestStarterFindUniqueArgs";
import { DeleteGameObjectQuestStarterArgs } from "./DeleteGameObjectQuestStarterArgs";
import { GameObjectQuestStarterService } from "../gameObjectQuestStarter.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => GameObjectQuestStarter)
export class GameObjectQuestStarterResolverBase {
  constructor(
    protected readonly service: GameObjectQuestStarterService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "GameObjectQuestStarter",
    action: "read",
    possession: "any",
  })
  async _gameObjectQuestStartersMeta(
    @graphql.Args() args: GameObjectQuestStarterCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [GameObjectQuestStarter])
  @nestAccessControl.UseRoles({
    resource: "GameObjectQuestStarter",
    action: "read",
    possession: "any",
  })
  async gameObjectQuestStarters(
    @graphql.Args() args: GameObjectQuestStarterFindManyArgs
  ): Promise<GameObjectQuestStarter[]> {
    return this.service.gameObjectQuestStarters(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => GameObjectQuestStarter, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "GameObjectQuestStarter",
    action: "read",
    possession: "own",
  })
  async gameObjectQuestStarter(
    @graphql.Args() args: GameObjectQuestStarterFindUniqueArgs
  ): Promise<GameObjectQuestStarter | null> {
    const result = await this.service.gameObjectQuestStarter(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => GameObjectQuestStarter)
  @nestAccessControl.UseRoles({
    resource: "GameObjectQuestStarter",
    action: "delete",
    possession: "any",
  })
  async deleteGameObjectQuestStarter(
    @graphql.Args() args: DeleteGameObjectQuestStarterArgs
  ): Promise<GameObjectQuestStarter | null> {
    try {
      return await this.service.deleteGameObjectQuestStarter(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
