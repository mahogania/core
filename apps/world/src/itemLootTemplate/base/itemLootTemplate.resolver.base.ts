/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ItemLootTemplate } from "./ItemLootTemplate";
import { ItemLootTemplateCountArgs } from "./ItemLootTemplateCountArgs";
import { ItemLootTemplateFindManyArgs } from "./ItemLootTemplateFindManyArgs";
import { ItemLootTemplateFindUniqueArgs } from "./ItemLootTemplateFindUniqueArgs";
import { DeleteItemLootTemplateArgs } from "./DeleteItemLootTemplateArgs";
import { ItemLootTemplateService } from "../itemLootTemplate.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ItemLootTemplate)
export class ItemLootTemplateResolverBase {
  constructor(
    protected readonly service: ItemLootTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ItemLootTemplate",
    action: "read",
    possession: "any",
  })
  async _itemLootTemplatesMeta(
    @graphql.Args() args: ItemLootTemplateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ItemLootTemplate])
  @nestAccessControl.UseRoles({
    resource: "ItemLootTemplate",
    action: "read",
    possession: "any",
  })
  async itemLootTemplates(
    @graphql.Args() args: ItemLootTemplateFindManyArgs
  ): Promise<ItemLootTemplate[]> {
    return this.service.itemLootTemplates(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ItemLootTemplate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ItemLootTemplate",
    action: "read",
    possession: "own",
  })
  async itemLootTemplate(
    @graphql.Args() args: ItemLootTemplateFindUniqueArgs
  ): Promise<ItemLootTemplate | null> {
    const result = await this.service.itemLootTemplate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ItemLootTemplate)
  @nestAccessControl.UseRoles({
    resource: "ItemLootTemplate",
    action: "delete",
    possession: "any",
  })
  async deleteItemLootTemplate(
    @graphql.Args() args: DeleteItemLootTemplateArgs
  ): Promise<ItemLootTemplate | null> {
    try {
      return await this.service.deleteItemLootTemplate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
