/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { Choice } from "./Choice";
import { ChoiceCountArgs } from "./ChoiceCountArgs";
import { ChoiceFindManyArgs } from "./ChoiceFindManyArgs";
import { ChoiceFindUniqueArgs } from "./ChoiceFindUniqueArgs";
import { CreateChoiceArgs } from "./CreateChoiceArgs";
import { UpdateChoiceArgs } from "./UpdateChoiceArgs";
import { DeleteChoiceArgs } from "./DeleteChoiceArgs";
import { ChoiceLocale } from "../../choiceLocale/base/ChoiceLocale";
import { Response } from "../../response/base/Response";
import { ChoiceService } from "../choice.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => Choice)
export class ChoiceResolverBase {
  constructor(
    protected readonly service: ChoiceService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "Choice",
    action: "read",
    possession: "any",
  })
  async _choicesMeta(
    @graphql.Args() args: ChoiceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [Choice])
  @nestAccessControl.UseRoles({
    resource: "Choice",
    action: "read",
    possession: "any",
  })
  async choices(@graphql.Args() args: ChoiceFindManyArgs): Promise<Choice[]> {
    return this.service.choices(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => Choice, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "Choice",
    action: "read",
    possession: "own",
  })
  async choice(
    @graphql.Args() args: ChoiceFindUniqueArgs
  ): Promise<Choice | null> {
    const result = await this.service.choice(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Choice)
  @nestAccessControl.UseRoles({
    resource: "Choice",
    action: "create",
    possession: "any",
  })
  async createChoice(@graphql.Args() args: CreateChoiceArgs): Promise<Choice> {
    return await this.service.createChoice({
      ...args,
      data: {
        ...args.data,

        choiceLocales: args.data.choiceLocales
          ? {
              connect: args.data.choiceLocales,
            }
          : undefined,

        responses: {
          connect: args.data.responses,
        },
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => Choice)
  @nestAccessControl.UseRoles({
    resource: "Choice",
    action: "update",
    possession: "any",
  })
  async updateChoice(
    @graphql.Args() args: UpdateChoiceArgs
  ): Promise<Choice | null> {
    try {
      return await this.service.updateChoice({
        ...args,
        data: {
          ...args.data,

          choiceLocales: args.data.choiceLocales
            ? {
                connect: args.data.choiceLocales,
              }
            : undefined,

          responses: {
            connect: args.data.responses,
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Choice)
  @nestAccessControl.UseRoles({
    resource: "Choice",
    action: "delete",
    possession: "any",
  })
  async deleteChoice(
    @graphql.Args() args: DeleteChoiceArgs
  ): Promise<Choice | null> {
    try {
      return await this.service.deleteChoice(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => ChoiceLocale, {
    nullable: true,
    name: "choiceLocales",
  })
  @nestAccessControl.UseRoles({
    resource: "ChoiceLocale",
    action: "read",
    possession: "any",
  })
  async getChoiceLocales(
    @graphql.Parent() parent: Choice
  ): Promise<ChoiceLocale | null> {
    const result = await this.service.getChoiceLocales(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => Response, {
    nullable: true,
    name: "responses",
  })
  @nestAccessControl.UseRoles({
    resource: "Response",
    action: "read",
    possession: "any",
  })
  async getResponses(
    @graphql.Parent() parent: Choice
  ): Promise<Response | null> {
    const result = await this.service.getResponses(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
