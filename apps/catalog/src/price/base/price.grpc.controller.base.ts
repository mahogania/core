/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { GrpcMethod } from "@nestjs/microservices";
import { PriceService } from "../price.service";
import { PriceCreateInput } from "./PriceCreateInput";
import { PriceWhereInput } from "./PriceWhereInput";
import { PriceWhereUniqueInput } from "./PriceWhereUniqueInput";
import { PriceFindManyArgs } from "./PriceFindManyArgs";
import { PriceUpdateInput } from "./PriceUpdateInput";
import { Price } from "./Price";

export class PriceGrpcControllerBase {
  constructor(protected readonly service: PriceService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Price })
  @GrpcMethod("PriceService", "createPrice")
  async createPrice(@common.Body() data: PriceCreateInput): Promise<Price> {
    return await this.service.createPrice({
      data: {
        ...data,

        currency: data.currency
          ? {
              connect: data.currency,
            }
          : undefined,

        options: data.options
          ? {
              connect: data.options,
            }
          : undefined,
      },
      select: {
        createdAt: true,

        currency: {
          select: {
            id: true,
          },
        },

        id: true,

        options: {
          select: {
            id: true,
          },
        },

        priceRuleUrl: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Price] })
  @ApiNestedQuery(PriceFindManyArgs)
  @GrpcMethod("PriceService", "prices")
  async prices(@common.Req() request: Request): Promise<Price[]> {
    const args = plainToClass(PriceFindManyArgs, request.query);
    return this.service.prices({
      ...args,
      select: {
        createdAt: true,

        currency: {
          select: {
            id: true,
          },
        },

        id: true,

        options: {
          select: {
            id: true,
          },
        },

        priceRuleUrl: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Price })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("PriceService", "price")
  async price(
    @common.Param() params: PriceWhereUniqueInput
  ): Promise<Price | null> {
    const result = await this.service.price({
      where: params,
      select: {
        createdAt: true,

        currency: {
          select: {
            id: true,
          },
        },

        id: true,

        options: {
          select: {
            id: true,
          },
        },

        priceRuleUrl: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Price })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("PriceService", "updatePrice")
  async updatePrice(
    @common.Param() params: PriceWhereUniqueInput,
    @common.Body() data: PriceUpdateInput
  ): Promise<Price | null> {
    try {
      return await this.service.updatePrice({
        where: params,
        data: {
          ...data,

          currency: data.currency
            ? {
                connect: data.currency,
              }
            : undefined,

          options: data.options
            ? {
                connect: data.options,
              }
            : undefined,
        },
        select: {
          createdAt: true,

          currency: {
            select: {
              id: true,
            },
          },

          id: true,

          options: {
            select: {
              id: true,
            },
          },

          priceRuleUrl: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Price })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("PriceService", "deletePrice")
  async deletePrice(
    @common.Param() params: PriceWhereUniqueInput
  ): Promise<Price | null> {
    try {
      return await this.service.deletePrice({
        where: params,
        select: {
          createdAt: true,

          currency: {
            select: {
              id: true,
            },
          },

          id: true,

          options: {
            select: {
              id: true,
            },
          },

          priceRuleUrl: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
