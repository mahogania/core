/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { Price } from "./Price";
import { PriceCountArgs } from "./PriceCountArgs";
import { PriceFindManyArgs } from "./PriceFindManyArgs";
import { PriceFindUniqueArgs } from "./PriceFindUniqueArgs";
import { CreatePriceArgs } from "./CreatePriceArgs";
import { UpdatePriceArgs } from "./UpdatePriceArgs";
import { DeletePriceArgs } from "./DeletePriceArgs";
import { Currency } from "../../currency/base/Currency";
import { Option } from "../../option/base/Option";
import { PriceService } from "../price.service";
@graphql.Resolver(() => Price)
export class PriceResolverBase {
  constructor(protected readonly service: PriceService) {}

  async _pricesMeta(
    @graphql.Args() args: PriceCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [Price])
  async prices(@graphql.Args() args: PriceFindManyArgs): Promise<Price[]> {
    return this.service.prices(args);
  }

  @graphql.Query(() => Price, { nullable: true })
  async price(
    @graphql.Args() args: PriceFindUniqueArgs
  ): Promise<Price | null> {
    const result = await this.service.price(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => Price)
  async createPrice(@graphql.Args() args: CreatePriceArgs): Promise<Price> {
    return await this.service.createPrice({
      ...args,
      data: {
        ...args.data,

        currency: args.data.currency
          ? {
              connect: args.data.currency,
            }
          : undefined,

        options: args.data.options
          ? {
              connect: args.data.options,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => Price)
  async updatePrice(
    @graphql.Args() args: UpdatePriceArgs
  ): Promise<Price | null> {
    try {
      return await this.service.updatePrice({
        ...args,
        data: {
          ...args.data,

          currency: args.data.currency
            ? {
                connect: args.data.currency,
              }
            : undefined,

          options: args.data.options
            ? {
                connect: args.data.options,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => Price)
  async deletePrice(
    @graphql.Args() args: DeletePriceArgs
  ): Promise<Price | null> {
    try {
      return await this.service.deletePrice(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Currency, {
    nullable: true,
    name: "currency",
  })
  async getCurrency(@graphql.Parent() parent: Price): Promise<Currency | null> {
    const result = await this.service.getCurrency(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => Option, {
    nullable: true,
    name: "options",
  })
  async getOptions(@graphql.Parent() parent: Price): Promise<Option | null> {
    const result = await this.service.getOptions(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
