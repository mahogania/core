/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { GrpcMethod } from "@nestjs/microservices";
import { QuoteService } from "../quote.service";
import { QuoteCreateInput } from "./QuoteCreateInput";
import { QuoteWhereInput } from "./QuoteWhereInput";
import { QuoteWhereUniqueInput } from "./QuoteWhereUniqueInput";
import { QuoteFindManyArgs } from "./QuoteFindManyArgs";
import { QuoteUpdateInput } from "./QuoteUpdateInput";
import { Quote } from "./Quote";
import { ItemFindManyArgs } from "../../item/base/ItemFindManyArgs";
import { Item } from "../../item/base/Item";
import { ItemWhereUniqueInput } from "../../item/base/ItemWhereUniqueInput";

export class QuoteGrpcControllerBase {
  constructor(protected readonly service: QuoteService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Quote })
  @GrpcMethod("QuoteService", "createQuote")
  async createQuote(@common.Body() data: QuoteCreateInput): Promise<Quote> {
    return await this.service.createQuote({
      data: {
        ...data,

        proposal: data.proposal
          ? {
              connect: data.proposal,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        id: true,

        proposal: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Quote] })
  @ApiNestedQuery(QuoteFindManyArgs)
  @GrpcMethod("QuoteService", "quotes")
  async quotes(@common.Req() request: Request): Promise<Quote[]> {
    const args = plainToClass(QuoteFindManyArgs, request.query);
    return this.service.quotes({
      ...args,
      select: {
        createdAt: true,
        id: true,

        proposal: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Quote })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("QuoteService", "quote")
  async quote(
    @common.Param() params: QuoteWhereUniqueInput
  ): Promise<Quote | null> {
    const result = await this.service.quote({
      where: params,
      select: {
        createdAt: true,
        id: true,

        proposal: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Quote })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("QuoteService", "updateQuote")
  async updateQuote(
    @common.Param() params: QuoteWhereUniqueInput,
    @common.Body() data: QuoteUpdateInput
  ): Promise<Quote | null> {
    try {
      return await this.service.updateQuote({
        where: params,
        data: {
          ...data,

          proposal: data.proposal
            ? {
                connect: data.proposal,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          id: true,

          proposal: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Quote })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("QuoteService", "deleteQuote")
  async deleteQuote(
    @common.Param() params: QuoteWhereUniqueInput
  ): Promise<Quote | null> {
    try {
      return await this.service.deleteQuote({
        where: params,
        select: {
          createdAt: true,
          id: true,

          proposal: {
            select: {
              id: true,
            },
          },

          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/lineItems")
  @ApiNestedQuery(ItemFindManyArgs)
  @GrpcMethod("QuoteService", "findManyLineItems")
  async findManyLineItems(
    @common.Req() request: Request,
    @common.Param() params: QuoteWhereUniqueInput
  ): Promise<Item[]> {
    const query = plainToClass(ItemFindManyArgs, request.query);
    const results = await this.service.findLineItems(params.id, {
      ...query,
      select: {
        configuration: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,

        quote: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/lineItems")
  @GrpcMethod("QuoteService", "connectLineItems")
  async connectLineItems(
    @common.Param() params: QuoteWhereUniqueInput,
    @common.Body() body: ItemWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      lineItems: {
        connect: body,
      },
    };
    await this.service.updateQuote({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/lineItems")
  @GrpcMethod("QuoteService", "updateLineItems")
  async updateLineItems(
    @common.Param() params: QuoteWhereUniqueInput,
    @common.Body() body: ItemWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      lineItems: {
        set: body,
      },
    };
    await this.service.updateQuote({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/lineItems")
  @GrpcMethod("QuoteService", "disconnectLineItems")
  async disconnectLineItems(
    @common.Param() params: QuoteWhereUniqueInput,
    @common.Body() body: ItemWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      lineItems: {
        disconnect: body,
      },
    };
    await this.service.updateQuote({
      where: params,
      data,
      select: { id: true },
    });
  }
}
