/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { WorkOrderOperation } from "./WorkOrderOperation";
import { WorkOrderOperationCountArgs } from "./WorkOrderOperationCountArgs";
import { WorkOrderOperationFindManyArgs } from "./WorkOrderOperationFindManyArgs";
import { WorkOrderOperationFindUniqueArgs } from "./WorkOrderOperationFindUniqueArgs";
import { DeleteWorkOrderOperationArgs } from "./DeleteWorkOrderOperationArgs";
import { WorkOrderOperationService } from "../workOrderOperation.service";
@graphql.Resolver(() => WorkOrderOperation)
export class WorkOrderOperationResolverBase {
  constructor(protected readonly service: WorkOrderOperationService) {}

  async _workOrderOperationsMeta(
    @graphql.Args() args: WorkOrderOperationCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [WorkOrderOperation])
  async workOrderOperations(
    @graphql.Args() args: WorkOrderOperationFindManyArgs
  ): Promise<WorkOrderOperation[]> {
    return this.service.workOrderOperations(args);
  }

  @graphql.Query(() => WorkOrderOperation, { nullable: true })
  async workOrderOperation(
    @graphql.Args() args: WorkOrderOperationFindUniqueArgs
  ): Promise<WorkOrderOperation | null> {
    const result = await this.service.workOrderOperation(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => WorkOrderOperation)
  async deleteWorkOrderOperation(
    @graphql.Args() args: DeleteWorkOrderOperationArgs
  ): Promise<WorkOrderOperation | null> {
    try {
      return await this.service.deleteWorkOrderOperation(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
