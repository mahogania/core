/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { MaterialRequestItem } from "./MaterialRequestItem";
import { MaterialRequestItemCountArgs } from "./MaterialRequestItemCountArgs";
import { MaterialRequestItemFindManyArgs } from "./MaterialRequestItemFindManyArgs";
import { MaterialRequestItemFindUniqueArgs } from "./MaterialRequestItemFindUniqueArgs";
import { DeleteMaterialRequestItemArgs } from "./DeleteMaterialRequestItemArgs";
import { MaterialRequestItemService } from "../materialRequestItem.service";
@graphql.Resolver(() => MaterialRequestItem)
export class MaterialRequestItemResolverBase {
  constructor(protected readonly service: MaterialRequestItemService) {}

  async _materialRequestItemsMeta(
    @graphql.Args() args: MaterialRequestItemCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [MaterialRequestItem])
  async materialRequestItems(
    @graphql.Args() args: MaterialRequestItemFindManyArgs
  ): Promise<MaterialRequestItem[]> {
    return this.service.materialRequestItems(args);
  }

  @graphql.Query(() => MaterialRequestItem, { nullable: true })
  async materialRequestItem(
    @graphql.Args() args: MaterialRequestItemFindUniqueArgs
  ): Promise<MaterialRequestItem | null> {
    const result = await this.service.materialRequestItem(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => MaterialRequestItem)
  async deleteMaterialRequestItem(
    @graphql.Args() args: DeleteMaterialRequestItemArgs
  ): Promise<MaterialRequestItem | null> {
    try {
      return await this.service.deleteMaterialRequestItem(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
