/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { LocationProfile } from "./LocationProfile";
import { LocationProfileCountArgs } from "./LocationProfileCountArgs";
import { LocationProfileFindManyArgs } from "./LocationProfileFindManyArgs";
import { LocationProfileFindUniqueArgs } from "./LocationProfileFindUniqueArgs";
import { DeleteLocationProfileArgs } from "./DeleteLocationProfileArgs";
import { LocationProfileService } from "../locationProfile.service";
@graphql.Resolver(() => LocationProfile)
export class LocationProfileResolverBase {
  constructor(protected readonly service: LocationProfileService) {}

  async _locationProfilesMeta(
    @graphql.Args() args: LocationProfileCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [LocationProfile])
  async locationProfiles(
    @graphql.Args() args: LocationProfileFindManyArgs
  ): Promise<LocationProfile[]> {
    return this.service.locationProfiles(args);
  }

  @graphql.Query(() => LocationProfile, { nullable: true })
  async locationProfile(
    @graphql.Args() args: LocationProfileFindUniqueArgs
  ): Promise<LocationProfile | null> {
    const result = await this.service.locationProfile(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => LocationProfile)
  async deleteLocationProfile(
    @graphql.Args() args: DeleteLocationProfileArgs
  ): Promise<LocationProfile | null> {
    try {
      return await this.service.deleteLocationProfile(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
