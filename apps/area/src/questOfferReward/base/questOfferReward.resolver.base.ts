/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { QuestOfferReward } from "./QuestOfferReward";
import { QuestOfferRewardCountArgs } from "./QuestOfferRewardCountArgs";
import { QuestOfferRewardFindManyArgs } from "./QuestOfferRewardFindManyArgs";
import { QuestOfferRewardFindUniqueArgs } from "./QuestOfferRewardFindUniqueArgs";
import { DeleteQuestOfferRewardArgs } from "./DeleteQuestOfferRewardArgs";
import { QuestOfferRewardService } from "../questOfferReward.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => QuestOfferReward)
export class QuestOfferRewardResolverBase {
  constructor(
    protected readonly service: QuestOfferRewardService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "read",
    possession: "any",
  })
  async _questOfferRewardsMeta(
    @graphql.Args() args: QuestOfferRewardCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [QuestOfferReward])
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "read",
    possession: "any",
  })
  async questOfferRewards(
    @graphql.Args() args: QuestOfferRewardFindManyArgs
  ): Promise<QuestOfferReward[]> {
    return this.service.questOfferRewards(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => QuestOfferReward, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "read",
    possession: "own",
  })
  async questOfferReward(
    @graphql.Args() args: QuestOfferRewardFindUniqueArgs
  ): Promise<QuestOfferReward | null> {
    const result = await this.service.questOfferReward(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => QuestOfferReward)
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "delete",
    possession: "any",
  })
  async deleteQuestOfferReward(
    @graphql.Args() args: DeleteQuestOfferRewardArgs
  ): Promise<QuestOfferReward | null> {
    try {
      return await this.service.deleteQuestOfferReward(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
