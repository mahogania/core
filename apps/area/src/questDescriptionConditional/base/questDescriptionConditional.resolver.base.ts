/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { QuestDescriptionConditional } from "./QuestDescriptionConditional";
import { QuestDescriptionConditionalCountArgs } from "./QuestDescriptionConditionalCountArgs";
import { QuestDescriptionConditionalFindManyArgs } from "./QuestDescriptionConditionalFindManyArgs";
import { QuestDescriptionConditionalFindUniqueArgs } from "./QuestDescriptionConditionalFindUniqueArgs";
import { DeleteQuestDescriptionConditionalArgs } from "./DeleteQuestDescriptionConditionalArgs";
import { QuestDescriptionConditionalService } from "../questDescriptionConditional.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => QuestDescriptionConditional)
export class QuestDescriptionConditionalResolverBase {
  constructor(
    protected readonly service: QuestDescriptionConditionalService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestDescriptionConditional",
    action: "read",
    possession: "any",
  })
  async _questDescriptionConditionalsMeta(
    @graphql.Args() args: QuestDescriptionConditionalCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [QuestDescriptionConditional])
  @nestAccessControl.UseRoles({
    resource: "QuestDescriptionConditional",
    action: "read",
    possession: "any",
  })
  async questDescriptionConditionals(
    @graphql.Args() args: QuestDescriptionConditionalFindManyArgs
  ): Promise<QuestDescriptionConditional[]> {
    return this.service.questDescriptionConditionals(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => QuestDescriptionConditional, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestDescriptionConditional",
    action: "read",
    possession: "own",
  })
  async questDescriptionConditional(
    @graphql.Args() args: QuestDescriptionConditionalFindUniqueArgs
  ): Promise<QuestDescriptionConditional | null> {
    const result = await this.service.questDescriptionConditional(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => QuestDescriptionConditional)
  @nestAccessControl.UseRoles({
    resource: "QuestDescriptionConditional",
    action: "delete",
    possession: "any",
  })
  async deleteQuestDescriptionConditional(
    @graphql.Args() args: DeleteQuestDescriptionConditionalArgs
  ): Promise<QuestDescriptionConditional | null> {
    try {
      return await this.service.deleteQuestDescriptionConditional(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
