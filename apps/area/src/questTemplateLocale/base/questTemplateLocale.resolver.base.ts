/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { QuestTemplateLocale } from "./QuestTemplateLocale";
import { QuestTemplateLocaleCountArgs } from "./QuestTemplateLocaleCountArgs";
import { QuestTemplateLocaleFindManyArgs } from "./QuestTemplateLocaleFindManyArgs";
import { QuestTemplateLocaleFindUniqueArgs } from "./QuestTemplateLocaleFindUniqueArgs";
import { DeleteQuestTemplateLocaleArgs } from "./DeleteQuestTemplateLocaleArgs";
import { QuestTemplateLocaleService } from "../questTemplateLocale.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => QuestTemplateLocale)
export class QuestTemplateLocaleResolverBase {
  constructor(
    protected readonly service: QuestTemplateLocaleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestTemplateLocale",
    action: "read",
    possession: "any",
  })
  async _questTemplateLocalesMeta(
    @graphql.Args() args: QuestTemplateLocaleCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [QuestTemplateLocale])
  @nestAccessControl.UseRoles({
    resource: "QuestTemplateLocale",
    action: "read",
    possession: "any",
  })
  async questTemplateLocales(
    @graphql.Args() args: QuestTemplateLocaleFindManyArgs
  ): Promise<QuestTemplateLocale[]> {
    return this.service.questTemplateLocales(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => QuestTemplateLocale, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestTemplateLocale",
    action: "read",
    possession: "own",
  })
  async questTemplateLocale(
    @graphql.Args() args: QuestTemplateLocaleFindUniqueArgs
  ): Promise<QuestTemplateLocale | null> {
    const result = await this.service.questTemplateLocale(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => QuestTemplateLocale)
  @nestAccessControl.UseRoles({
    resource: "QuestTemplateLocale",
    action: "delete",
    possession: "any",
  })
  async deleteQuestTemplateLocale(
    @graphql.Args() args: DeleteQuestTemplateLocaleArgs
  ): Promise<QuestTemplateLocale | null> {
    try {
      return await this.service.deleteQuestTemplateLocale(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
