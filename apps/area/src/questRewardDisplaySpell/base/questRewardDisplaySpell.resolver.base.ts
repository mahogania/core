/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { QuestRewardDisplaySpell } from "./QuestRewardDisplaySpell";
import { QuestRewardDisplaySpellCountArgs } from "./QuestRewardDisplaySpellCountArgs";
import { QuestRewardDisplaySpellFindManyArgs } from "./QuestRewardDisplaySpellFindManyArgs";
import { QuestRewardDisplaySpellFindUniqueArgs } from "./QuestRewardDisplaySpellFindUniqueArgs";
import { DeleteQuestRewardDisplaySpellArgs } from "./DeleteQuestRewardDisplaySpellArgs";
import { QuestRewardDisplaySpellService } from "../questRewardDisplaySpell.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => QuestRewardDisplaySpell)
export class QuestRewardDisplaySpellResolverBase {
  constructor(
    protected readonly service: QuestRewardDisplaySpellService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestRewardDisplaySpell",
    action: "read",
    possession: "any",
  })
  async _questRewardDisplaySpellsMeta(
    @graphql.Args() args: QuestRewardDisplaySpellCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [QuestRewardDisplaySpell])
  @nestAccessControl.UseRoles({
    resource: "QuestRewardDisplaySpell",
    action: "read",
    possession: "any",
  })
  async questRewardDisplaySpells(
    @graphql.Args() args: QuestRewardDisplaySpellFindManyArgs
  ): Promise<QuestRewardDisplaySpell[]> {
    return this.service.questRewardDisplaySpells(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => QuestRewardDisplaySpell, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestRewardDisplaySpell",
    action: "read",
    possession: "own",
  })
  async questRewardDisplaySpell(
    @graphql.Args() args: QuestRewardDisplaySpellFindUniqueArgs
  ): Promise<QuestRewardDisplaySpell | null> {
    const result = await this.service.questRewardDisplaySpell(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => QuestRewardDisplaySpell)
  @nestAccessControl.UseRoles({
    resource: "QuestRewardDisplaySpell",
    action: "delete",
    possession: "any",
  })
  async deleteQuestRewardDisplaySpell(
    @graphql.Args() args: DeleteQuestRewardDisplaySpellArgs
  ): Promise<QuestRewardDisplaySpell | null> {
    try {
      return await this.service.deleteQuestRewardDisplaySpell(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
