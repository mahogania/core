/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { QuestPoiPoints } from "./QuestPoiPoints";
import { QuestPoiPointsCountArgs } from "./QuestPoiPointsCountArgs";
import { QuestPoiPointsFindManyArgs } from "./QuestPoiPointsFindManyArgs";
import { QuestPoiPointsFindUniqueArgs } from "./QuestPoiPointsFindUniqueArgs";
import { DeleteQuestPoiPointsArgs } from "./DeleteQuestPoiPointsArgs";
import { QuestPoiPointsService } from "../questPoiPoints.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => QuestPoiPoints)
export class QuestPoiPointsResolverBase {
  constructor(
    protected readonly service: QuestPoiPointsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestPoiPoints",
    action: "read",
    possession: "any",
  })
  async _questPoiPointsItemsMeta(
    @graphql.Args() args: QuestPoiPointsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [QuestPoiPoints])
  @nestAccessControl.UseRoles({
    resource: "QuestPoiPoints",
    action: "read",
    possession: "any",
  })
  async questPoiPointsItems(
    @graphql.Args() args: QuestPoiPointsFindManyArgs
  ): Promise<QuestPoiPoints[]> {
    return this.service.questPoiPointsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => QuestPoiPoints, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestPoiPoints",
    action: "read",
    possession: "own",
  })
  async questPoiPoints(
    @graphql.Args() args: QuestPoiPointsFindUniqueArgs
  ): Promise<QuestPoiPoints | null> {
    const result = await this.service.questPoiPoints(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => QuestPoiPoints)
  @nestAccessControl.UseRoles({
    resource: "QuestPoiPoints",
    action: "delete",
    possession: "any",
  })
  async deleteQuestPoiPoints(
    @graphql.Args() args: DeleteQuestPoiPointsArgs
  ): Promise<QuestPoiPoints | null> {
    try {
      return await this.service.deleteQuestPoiPoints(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
