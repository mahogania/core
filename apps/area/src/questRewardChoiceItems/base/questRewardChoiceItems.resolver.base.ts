/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { QuestRewardChoiceItems } from "./QuestRewardChoiceItems";
import { QuestRewardChoiceItemsCountArgs } from "./QuestRewardChoiceItemsCountArgs";
import { QuestRewardChoiceItemsFindManyArgs } from "./QuestRewardChoiceItemsFindManyArgs";
import { QuestRewardChoiceItemsFindUniqueArgs } from "./QuestRewardChoiceItemsFindUniqueArgs";
import { DeleteQuestRewardChoiceItemsArgs } from "./DeleteQuestRewardChoiceItemsArgs";
import { QuestRewardChoiceItemsService } from "../questRewardChoiceItems.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => QuestRewardChoiceItems)
export class QuestRewardChoiceItemsResolverBase {
  constructor(
    protected readonly service: QuestRewardChoiceItemsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestRewardChoiceItems",
    action: "read",
    possession: "any",
  })
  async _questRewardChoiceItemsItemsMeta(
    @graphql.Args() args: QuestRewardChoiceItemsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [QuestRewardChoiceItems])
  @nestAccessControl.UseRoles({
    resource: "QuestRewardChoiceItems",
    action: "read",
    possession: "any",
  })
  async questRewardChoiceItemsItems(
    @graphql.Args() args: QuestRewardChoiceItemsFindManyArgs
  ): Promise<QuestRewardChoiceItems[]> {
    return this.service.questRewardChoiceItemsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => QuestRewardChoiceItems, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestRewardChoiceItems",
    action: "read",
    possession: "own",
  })
  async questRewardChoiceItems(
    @graphql.Args() args: QuestRewardChoiceItemsFindUniqueArgs
  ): Promise<QuestRewardChoiceItems | null> {
    const result = await this.service.questRewardChoiceItems(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => QuestRewardChoiceItems)
  @nestAccessControl.UseRoles({
    resource: "QuestRewardChoiceItems",
    action: "delete",
    possession: "any",
  })
  async deleteQuestRewardChoiceItems(
    @graphql.Args() args: DeleteQuestRewardChoiceItemsArgs
  ): Promise<QuestRewardChoiceItems | null> {
    try {
      return await this.service.deleteQuestRewardChoiceItems(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
