/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { QuestMailSender } from "./QuestMailSender";
import { QuestMailSenderCountArgs } from "./QuestMailSenderCountArgs";
import { QuestMailSenderFindManyArgs } from "./QuestMailSenderFindManyArgs";
import { QuestMailSenderFindUniqueArgs } from "./QuestMailSenderFindUniqueArgs";
import { DeleteQuestMailSenderArgs } from "./DeleteQuestMailSenderArgs";
import { QuestMailSenderService } from "../questMailSender.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => QuestMailSender)
export class QuestMailSenderResolverBase {
  constructor(
    protected readonly service: QuestMailSenderService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestMailSender",
    action: "read",
    possession: "any",
  })
  async _questMailSendersMeta(
    @graphql.Args() args: QuestMailSenderCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [QuestMailSender])
  @nestAccessControl.UseRoles({
    resource: "QuestMailSender",
    action: "read",
    possession: "any",
  })
  async questMailSenders(
    @graphql.Args() args: QuestMailSenderFindManyArgs
  ): Promise<QuestMailSender[]> {
    return this.service.questMailSenders(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => QuestMailSender, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestMailSender",
    action: "read",
    possession: "own",
  })
  async questMailSender(
    @graphql.Args() args: QuestMailSenderFindUniqueArgs
  ): Promise<QuestMailSender | null> {
    const result = await this.service.questMailSender(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => QuestMailSender)
  @nestAccessControl.UseRoles({
    resource: "QuestMailSender",
    action: "delete",
    possession: "any",
  })
  async deleteQuestMailSender(
    @graphql.Args() args: DeleteQuestMailSenderArgs
  ): Promise<QuestMailSender | null> {
    try {
      return await this.service.deleteQuestMailSender(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
