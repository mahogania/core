/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ScenarioPoi } from "./ScenarioPoi";
import { ScenarioPoiCountArgs } from "./ScenarioPoiCountArgs";
import { ScenarioPoiFindManyArgs } from "./ScenarioPoiFindManyArgs";
import { ScenarioPoiFindUniqueArgs } from "./ScenarioPoiFindUniqueArgs";
import { DeleteScenarioPoiArgs } from "./DeleteScenarioPoiArgs";
import { ScenarioPoiService } from "../scenarioPoi.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ScenarioPoi)
export class ScenarioPoiResolverBase {
  constructor(
    protected readonly service: ScenarioPoiService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "read",
    possession: "any",
  })
  async _scenarioPoisMeta(
    @graphql.Args() args: ScenarioPoiCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ScenarioPoi])
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "read",
    possession: "any",
  })
  async scenarioPois(
    @graphql.Args() args: ScenarioPoiFindManyArgs
  ): Promise<ScenarioPoi[]> {
    return this.service.scenarioPois(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ScenarioPoi, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "read",
    possession: "own",
  })
  async scenarioPoi(
    @graphql.Args() args: ScenarioPoiFindUniqueArgs
  ): Promise<ScenarioPoi | null> {
    const result = await this.service.scenarioPoi(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => ScenarioPoi)
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "delete",
    possession: "any",
  })
  async deleteScenarioPoi(
    @graphql.Args() args: DeleteScenarioPoiArgs
  ): Promise<ScenarioPoi | null> {
    try {
      return await this.service.deleteScenarioPoi(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
