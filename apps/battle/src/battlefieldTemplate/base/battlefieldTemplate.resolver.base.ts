/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BattlefieldTemplate } from "./BattlefieldTemplate";
import { BattlefieldTemplateCountArgs } from "./BattlefieldTemplateCountArgs";
import { BattlefieldTemplateFindManyArgs } from "./BattlefieldTemplateFindManyArgs";
import { BattlefieldTemplateFindUniqueArgs } from "./BattlefieldTemplateFindUniqueArgs";
import { DeleteBattlefieldTemplateArgs } from "./DeleteBattlefieldTemplateArgs";
import { BattlefieldTemplateService } from "../battlefieldTemplate.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => BattlefieldTemplate)
export class BattlefieldTemplateResolverBase {
  constructor(
    protected readonly service: BattlefieldTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "BattlefieldTemplate",
    action: "read",
    possession: "any",
  })
  async _battlefieldTemplatesMeta(
    @graphql.Args() args: BattlefieldTemplateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [BattlefieldTemplate])
  @nestAccessControl.UseRoles({
    resource: "BattlefieldTemplate",
    action: "read",
    possession: "any",
  })
  async battlefieldTemplates(
    @graphql.Args() args: BattlefieldTemplateFindManyArgs
  ): Promise<BattlefieldTemplate[]> {
    return this.service.battlefieldTemplates(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => BattlefieldTemplate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "BattlefieldTemplate",
    action: "read",
    possession: "own",
  })
  async battlefieldTemplate(
    @graphql.Args() args: BattlefieldTemplateFindUniqueArgs
  ): Promise<BattlefieldTemplate | null> {
    const result = await this.service.battlefieldTemplate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => BattlefieldTemplate)
  @nestAccessControl.UseRoles({
    resource: "BattlefieldTemplate",
    action: "delete",
    possession: "any",
  })
  async deleteBattlefieldTemplate(
    @graphql.Args() args: DeleteBattlefieldTemplateArgs
  ): Promise<BattlefieldTemplate | null> {
    try {
      return await this.service.deleteBattlefieldTemplate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
