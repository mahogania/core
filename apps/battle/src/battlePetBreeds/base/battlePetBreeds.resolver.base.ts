/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { BattlePetBreeds } from "./BattlePetBreeds";
import { BattlePetBreedsCountArgs } from "./BattlePetBreedsCountArgs";
import { BattlePetBreedsFindManyArgs } from "./BattlePetBreedsFindManyArgs";
import { BattlePetBreedsFindUniqueArgs } from "./BattlePetBreedsFindUniqueArgs";
import { DeleteBattlePetBreedsArgs } from "./DeleteBattlePetBreedsArgs";
import { BattlePetBreedsService } from "../battlePetBreeds.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => BattlePetBreeds)
export class BattlePetBreedsResolverBase {
  constructor(
    protected readonly service: BattlePetBreedsService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "BattlePetBreeds",
    action: "read",
    possession: "any",
  })
  async _battlePetBreedsItemsMeta(
    @graphql.Args() args: BattlePetBreedsCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [BattlePetBreeds])
  @nestAccessControl.UseRoles({
    resource: "BattlePetBreeds",
    action: "read",
    possession: "any",
  })
  async battlePetBreedsItems(
    @graphql.Args() args: BattlePetBreedsFindManyArgs
  ): Promise<BattlePetBreeds[]> {
    return this.service.battlePetBreedsItems(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => BattlePetBreeds, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "BattlePetBreeds",
    action: "read",
    possession: "own",
  })
  async battlePetBreeds(
    @graphql.Args() args: BattlePetBreedsFindUniqueArgs
  ): Promise<BattlePetBreeds | null> {
    const result = await this.service.battlePetBreeds(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => BattlePetBreeds)
  @nestAccessControl.UseRoles({
    resource: "BattlePetBreeds",
    action: "delete",
    possession: "any",
  })
  async deleteBattlePetBreeds(
    @graphql.Args() args: DeleteBattlePetBreedsArgs
  ): Promise<BattlePetBreeds | null> {
    try {
      return await this.service.deleteBattlePetBreeds(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
