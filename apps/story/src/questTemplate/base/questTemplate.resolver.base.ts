/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { QuestTemplate } from "./QuestTemplate";
import { QuestTemplateCountArgs } from "./QuestTemplateCountArgs";
import { QuestTemplateFindManyArgs } from "./QuestTemplateFindManyArgs";
import { QuestTemplateFindUniqueArgs } from "./QuestTemplateFindUniqueArgs";
import { CreateQuestTemplateArgs } from "./CreateQuestTemplateArgs";
import { UpdateQuestTemplateArgs } from "./UpdateQuestTemplateArgs";
import { DeleteQuestTemplateArgs } from "./DeleteQuestTemplateArgs";
import { QuestFindManyArgs } from "../../quest/base/QuestFindManyArgs";
import { Quest } from "../../quest/base/Quest";
import { QuestMail } from "../../questMail/base/QuestMail";
import { QuestObjective } from "../../questObjective/base/QuestObjective";
import { QuestOfferReward } from "../../questOfferReward/base/QuestOfferReward";
import { QuestPool } from "../../questPool/base/QuestPool";
import { QuestRequestItem } from "../../questRequestItem/base/QuestRequestItem";
import { QuestTemplateLocale } from "../../questTemplateLocale/base/QuestTemplateLocale";
import { QuestTemplateService } from "../questTemplate.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => QuestTemplate)
export class QuestTemplateResolverBase {
  constructor(
    protected readonly service: QuestTemplateService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestTemplate",
    action: "read",
    possession: "any",
  })
  async _questTemplatesMeta(
    @graphql.Args() args: QuestTemplateCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [QuestTemplate])
  @nestAccessControl.UseRoles({
    resource: "QuestTemplate",
    action: "read",
    possession: "any",
  })
  async questTemplates(
    @graphql.Args() args: QuestTemplateFindManyArgs
  ): Promise<QuestTemplate[]> {
    return this.service.questTemplates(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => QuestTemplate, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestTemplate",
    action: "read",
    possession: "own",
  })
  async questTemplate(
    @graphql.Args() args: QuestTemplateFindUniqueArgs
  ): Promise<QuestTemplate | null> {
    const result = await this.service.questTemplate(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => QuestTemplate)
  @nestAccessControl.UseRoles({
    resource: "QuestTemplate",
    action: "create",
    possession: "any",
  })
  async createQuestTemplate(
    @graphql.Args() args: CreateQuestTemplateArgs
  ): Promise<QuestTemplate> {
    return await this.service.createQuestTemplate({
      ...args,
      data: {
        ...args.data,

        questMails: args.data.questMails
          ? {
              connect: args.data.questMails,
            }
          : undefined,

        questObjectives: {
          connect: args.data.questObjectives,
        },

        questOfferRewards: args.data.questOfferRewards
          ? {
              connect: args.data.questOfferRewards,
            }
          : undefined,

        questPools: args.data.questPools
          ? {
              connect: args.data.questPools,
            }
          : undefined,

        questRequestItems: args.data.questRequestItems
          ? {
              connect: args.data.questRequestItems,
            }
          : undefined,

        questTemplateLocale: args.data.questTemplateLocale
          ? {
              connect: args.data.questTemplateLocale,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => QuestTemplate)
  @nestAccessControl.UseRoles({
    resource: "QuestTemplate",
    action: "update",
    possession: "any",
  })
  async updateQuestTemplate(
    @graphql.Args() args: UpdateQuestTemplateArgs
  ): Promise<QuestTemplate | null> {
    try {
      return await this.service.updateQuestTemplate({
        ...args,
        data: {
          ...args.data,

          questMails: args.data.questMails
            ? {
                connect: args.data.questMails,
              }
            : undefined,

          questObjectives: {
            connect: args.data.questObjectives,
          },

          questOfferRewards: args.data.questOfferRewards
            ? {
                connect: args.data.questOfferRewards,
              }
            : undefined,

          questPools: args.data.questPools
            ? {
                connect: args.data.questPools,
              }
            : undefined,

          questRequestItems: args.data.questRequestItems
            ? {
                connect: args.data.questRequestItems,
              }
            : undefined,

          questTemplateLocale: args.data.questTemplateLocale
            ? {
                connect: args.data.questTemplateLocale,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => QuestTemplate)
  @nestAccessControl.UseRoles({
    resource: "QuestTemplate",
    action: "delete",
    possession: "any",
  })
  async deleteQuestTemplate(
    @graphql.Args() args: DeleteQuestTemplateArgs
  ): Promise<QuestTemplate | null> {
    try {
      return await this.service.deleteQuestTemplate(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [Quest], { name: "quests" })
  @nestAccessControl.UseRoles({
    resource: "Quest",
    action: "read",
    possession: "any",
  })
  async findQuests(
    @graphql.Parent() parent: QuestTemplate,
    @graphql.Args() args: QuestFindManyArgs
  ): Promise<Quest[]> {
    const results = await this.service.findQuests(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => QuestMail, {
    nullable: true,
    name: "questMails",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestMail",
    action: "read",
    possession: "any",
  })
  async getQuestMails(
    @graphql.Parent() parent: QuestTemplate
  ): Promise<QuestMail | null> {
    const result = await this.service.getQuestMails(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => QuestObjective, {
    nullable: true,
    name: "questObjectives",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestObjective",
    action: "read",
    possession: "any",
  })
  async getQuestObjectives(
    @graphql.Parent() parent: QuestTemplate
  ): Promise<QuestObjective | null> {
    const result = await this.service.getQuestObjectives(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => QuestOfferReward, {
    nullable: true,
    name: "questOfferRewards",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "read",
    possession: "any",
  })
  async getQuestOfferRewards(
    @graphql.Parent() parent: QuestTemplate
  ): Promise<QuestOfferReward | null> {
    const result = await this.service.getQuestOfferRewards(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => QuestPool, {
    nullable: true,
    name: "questPools",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestPool",
    action: "read",
    possession: "any",
  })
  async getQuestPools(
    @graphql.Parent() parent: QuestTemplate
  ): Promise<QuestPool | null> {
    const result = await this.service.getQuestPools(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => QuestRequestItem, {
    nullable: true,
    name: "questRequestItems",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestRequestItem",
    action: "read",
    possession: "any",
  })
  async getQuestRequestItems(
    @graphql.Parent() parent: QuestTemplate
  ): Promise<QuestRequestItem | null> {
    const result = await this.service.getQuestRequestItems(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => QuestTemplateLocale, {
    nullable: true,
    name: "questTemplateLocale",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestTemplateLocale",
    action: "read",
    possession: "any",
  })
  async getQuestTemplateLocale(
    @graphql.Parent() parent: QuestTemplate
  ): Promise<QuestTemplateLocale | null> {
    const result = await this.service.getQuestTemplateLocale(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
