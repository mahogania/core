/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { ScenarioPoi } from "./ScenarioPoi";
import { ScenarioPoiCountArgs } from "./ScenarioPoiCountArgs";
import { ScenarioPoiFindManyArgs } from "./ScenarioPoiFindManyArgs";
import { ScenarioPoiFindUniqueArgs } from "./ScenarioPoiFindUniqueArgs";
import { CreateScenarioPoiArgs } from "./CreateScenarioPoiArgs";
import { UpdateScenarioPoiArgs } from "./UpdateScenarioPoiArgs";
import { DeleteScenarioPoiArgs } from "./DeleteScenarioPoiArgs";
import { ScenarioRegionFindManyArgs } from "../../scenarioRegion/base/ScenarioRegionFindManyArgs";
import { ScenarioRegion } from "../../scenarioRegion/base/ScenarioRegion";
import { ScenarioPoiService } from "../scenarioPoi.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => ScenarioPoi)
export class ScenarioPoiResolverBase {
  constructor(
    protected readonly service: ScenarioPoiService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "read",
    possession: "any",
  })
  async _scenarioPoisMeta(
    @graphql.Args() args: ScenarioPoiCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [ScenarioPoi])
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "read",
    possession: "any",
  })
  async scenarioPois(
    @graphql.Args() args: ScenarioPoiFindManyArgs
  ): Promise<ScenarioPoi[]> {
    return this.service.scenarioPois(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => ScenarioPoi, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "read",
    possession: "own",
  })
  async scenarioPoi(
    @graphql.Args() args: ScenarioPoiFindUniqueArgs
  ): Promise<ScenarioPoi | null> {
    const result = await this.service.scenarioPoi(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => ScenarioPoi)
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "create",
    possession: "any",
  })
  async createScenarioPoi(
    @graphql.Args() args: CreateScenarioPoiArgs
  ): Promise<ScenarioPoi> {
    return await this.service.createScenarioPoi({
      ...args,
      data: args.data,
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => ScenarioPoi)
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "update",
    possession: "any",
  })
  async updateScenarioPoi(
    @graphql.Args() args: UpdateScenarioPoiArgs
  ): Promise<ScenarioPoi | null> {
    try {
      return await this.service.updateScenarioPoi({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => ScenarioPoi)
  @nestAccessControl.UseRoles({
    resource: "ScenarioPoi",
    action: "delete",
    possession: "any",
  })
  async deleteScenarioPoi(
    @graphql.Args() args: DeleteScenarioPoiArgs
  ): Promise<ScenarioPoi | null> {
    try {
      return await this.service.deleteScenarioPoi(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [ScenarioRegion], { name: "scenarioRegions" })
  @nestAccessControl.UseRoles({
    resource: "ScenarioRegion",
    action: "read",
    possession: "any",
  })
  async findScenarioRegions(
    @graphql.Parent() parent: ScenarioPoi,
    @graphql.Args() args: ScenarioRegionFindManyArgs
  ): Promise<ScenarioRegion[]> {
    const results = await this.service.findScenarioRegions(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }
}
