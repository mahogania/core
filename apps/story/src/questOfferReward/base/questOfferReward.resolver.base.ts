/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import * as nestAccessControl from "nest-access-control";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as common from "@nestjs/common";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { QuestOfferReward } from "./QuestOfferReward";
import { QuestOfferRewardCountArgs } from "./QuestOfferRewardCountArgs";
import { QuestOfferRewardFindManyArgs } from "./QuestOfferRewardFindManyArgs";
import { QuestOfferRewardFindUniqueArgs } from "./QuestOfferRewardFindUniqueArgs";
import { CreateQuestOfferRewardArgs } from "./CreateQuestOfferRewardArgs";
import { UpdateQuestOfferRewardArgs } from "./UpdateQuestOfferRewardArgs";
import { DeleteQuestOfferRewardArgs } from "./DeleteQuestOfferRewardArgs";
import { QuestOfferRewardConditionalFindManyArgs } from "../../questOfferRewardConditional/base/QuestOfferRewardConditionalFindManyArgs";
import { QuestOfferRewardConditional } from "../../questOfferRewardConditional/base/QuestOfferRewardConditional";
import { QuestOfferRewardLocaleFindManyArgs } from "../../questOfferRewardLocale/base/QuestOfferRewardLocaleFindManyArgs";
import { QuestOfferRewardLocale } from "../../questOfferRewardLocale/base/QuestOfferRewardLocale";
import { QuestTemplateFindManyArgs } from "../../questTemplate/base/QuestTemplateFindManyArgs";
import { QuestTemplate } from "../../questTemplate/base/QuestTemplate";
import { QuestRewardItem } from "../../questRewardItem/base/QuestRewardItem";
import { QuestRewardSpell } from "../../questRewardSpell/base/QuestRewardSpell";
import { QuestOfferRewardService } from "../questOfferReward.service";
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
@graphql.Resolver(() => QuestOfferReward)
export class QuestOfferRewardResolverBase {
  constructor(
    protected readonly service: QuestOfferRewardService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "read",
    possession: "any",
  })
  async _questOfferRewardsMeta(
    @graphql.Args() args: QuestOfferRewardCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [QuestOfferReward])
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "read",
    possession: "any",
  })
  async questOfferRewards(
    @graphql.Args() args: QuestOfferRewardFindManyArgs
  ): Promise<QuestOfferReward[]> {
    return this.service.questOfferRewards(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => QuestOfferReward, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "read",
    possession: "own",
  })
  async questOfferReward(
    @graphql.Args() args: QuestOfferRewardFindUniqueArgs
  ): Promise<QuestOfferReward | null> {
    const result = await this.service.questOfferReward(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => QuestOfferReward)
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "create",
    possession: "any",
  })
  async createQuestOfferReward(
    @graphql.Args() args: CreateQuestOfferRewardArgs
  ): Promise<QuestOfferReward> {
    return await this.service.createQuestOfferReward({
      ...args,
      data: {
        ...args.data,

        questRewardItem: args.data.questRewardItem
          ? {
              connect: args.data.questRewardItem,
            }
          : undefined,

        questRewardSpell: args.data.questRewardSpell
          ? {
              connect: args.data.questRewardSpell,
            }
          : undefined,
      },
    });
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @graphql.Mutation(() => QuestOfferReward)
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "update",
    possession: "any",
  })
  async updateQuestOfferReward(
    @graphql.Args() args: UpdateQuestOfferRewardArgs
  ): Promise<QuestOfferReward | null> {
    try {
      return await this.service.updateQuestOfferReward({
        ...args,
        data: {
          ...args.data,

          questRewardItem: args.data.questRewardItem
            ? {
                connect: args.data.questRewardItem,
              }
            : undefined,

          questRewardSpell: args.data.questRewardSpell
            ? {
                connect: args.data.questRewardSpell,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => QuestOfferReward)
  @nestAccessControl.UseRoles({
    resource: "QuestOfferReward",
    action: "delete",
    possession: "any",
  })
  async deleteQuestOfferReward(
    @graphql.Args() args: DeleteQuestOfferRewardArgs
  ): Promise<QuestOfferReward | null> {
    try {
      return await this.service.deleteQuestOfferReward(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [QuestOfferRewardConditional], {
    name: "questOfferRewardConditionals",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestOfferRewardConditional",
    action: "read",
    possession: "any",
  })
  async findQuestOfferRewardConditionals(
    @graphql.Parent() parent: QuestOfferReward,
    @graphql.Args() args: QuestOfferRewardConditionalFindManyArgs
  ): Promise<QuestOfferRewardConditional[]> {
    const results = await this.service.findQuestOfferRewardConditionals(
      parent.id,
      args
    );

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [QuestOfferRewardLocale], {
    name: "questOfferRewardLocales",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestOfferRewardLocale",
    action: "read",
    possession: "any",
  })
  async findQuestOfferRewardLocales(
    @graphql.Parent() parent: QuestOfferReward,
    @graphql.Args() args: QuestOfferRewardLocaleFindManyArgs
  ): Promise<QuestOfferRewardLocale[]> {
    const results = await this.service.findQuestOfferRewardLocales(
      parent.id,
      args
    );

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => [QuestTemplate], { name: "questTemplates" })
  @nestAccessControl.UseRoles({
    resource: "QuestTemplate",
    action: "read",
    possession: "any",
  })
  async findQuestTemplates(
    @graphql.Parent() parent: QuestOfferReward,
    @graphql.Args() args: QuestTemplateFindManyArgs
  ): Promise<QuestTemplate[]> {
    const results = await this.service.findQuestTemplates(parent.id, args);

    if (!results) {
      return [];
    }

    return results;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => QuestRewardItem, {
    nullable: true,
    name: "questRewardItem",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestRewardItem",
    action: "read",
    possession: "any",
  })
  async getQuestRewardItem(
    @graphql.Parent() parent: QuestOfferReward
  ): Promise<QuestRewardItem | null> {
    const result = await this.service.getQuestRewardItem(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.ResolveField(() => QuestRewardSpell, {
    nullable: true,
    name: "questRewardSpell",
  })
  @nestAccessControl.UseRoles({
    resource: "QuestRewardSpell",
    action: "read",
    possession: "any",
  })
  async getQuestRewardSpell(
    @graphql.Parent() parent: QuestOfferReward
  ): Promise<QuestRewardSpell | null> {
    const result = await this.service.getQuestRewardSpell(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
