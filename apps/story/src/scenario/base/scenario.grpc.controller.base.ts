/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { GrpcMethod } from "@nestjs/microservices";
import { ScenarioService } from "../scenario.service";
import { ScenarioCreateInput } from "./ScenarioCreateInput";
import { ScenarioWhereInput } from "./ScenarioWhereInput";
import { ScenarioWhereUniqueInput } from "./ScenarioWhereUniqueInput";
import { ScenarioFindManyArgs } from "./ScenarioFindManyArgs";
import { ScenarioUpdateInput } from "./ScenarioUpdateInput";
import { Scenario } from "./Scenario";
import { AreaScenarioTriggerFindManyArgs } from "../../areaScenarioTrigger/base/AreaScenarioTriggerFindManyArgs";
import { AreaScenarioTrigger } from "../../areaScenarioTrigger/base/AreaScenarioTrigger";
import { AreaScenarioTriggerWhereUniqueInput } from "../../areaScenarioTrigger/base/AreaScenarioTriggerWhereUniqueInput";

export class ScenarioGrpcControllerBase {
  constructor(protected readonly service: ScenarioService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Scenario })
  @GrpcMethod("ScenarioService", "createScenario")
  async createScenario(
    @common.Body() data: ScenarioCreateInput
  ): Promise<Scenario> {
    return await this.service.createScenario({
      data: {
        ...data,

        scenarioRegions: {
          connect: data.scenarioRegions,
        },

        sceneTemplate: {
          connect: data.sceneTemplate,
        },

        user: data.user
          ? {
              connect: data.user,
            }
          : undefined,
      },
      select: {
        createdAt: true,
        id: true,

        scenarioRegions: {
          select: {
            id: true,
          },
        },

        sceneTemplate: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Scenario] })
  @ApiNestedQuery(ScenarioFindManyArgs)
  @GrpcMethod("ScenarioService", "scenarios")
  async scenarios(@common.Req() request: Request): Promise<Scenario[]> {
    const args = plainToClass(ScenarioFindManyArgs, request.query);
    return this.service.scenarios({
      ...args,
      select: {
        createdAt: true,
        id: true,

        scenarioRegions: {
          select: {
            id: true,
          },
        },

        sceneTemplate: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Scenario })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("ScenarioService", "scenario")
  async scenario(
    @common.Param() params: ScenarioWhereUniqueInput
  ): Promise<Scenario | null> {
    const result = await this.service.scenario({
      where: params,
      select: {
        createdAt: true,
        id: true,

        scenarioRegions: {
          select: {
            id: true,
          },
        },

        sceneTemplate: {
          select: {
            id: true,
          },
        },

        updatedAt: true,

        user: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Scenario })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("ScenarioService", "updateScenario")
  async updateScenario(
    @common.Param() params: ScenarioWhereUniqueInput,
    @common.Body() data: ScenarioUpdateInput
  ): Promise<Scenario | null> {
    try {
      return await this.service.updateScenario({
        where: params,
        data: {
          ...data,

          scenarioRegions: {
            connect: data.scenarioRegions,
          },

          sceneTemplate: {
            connect: data.sceneTemplate,
          },

          user: data.user
            ? {
                connect: data.user,
              }
            : undefined,
        },
        select: {
          createdAt: true,
          id: true,

          scenarioRegions: {
            select: {
              id: true,
            },
          },

          sceneTemplate: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Scenario })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("ScenarioService", "deleteScenario")
  async deleteScenario(
    @common.Param() params: ScenarioWhereUniqueInput
  ): Promise<Scenario | null> {
    try {
      return await this.service.deleteScenario({
        where: params,
        select: {
          createdAt: true,
          id: true,

          scenarioRegions: {
            select: {
              id: true,
            },
          },

          sceneTemplate: {
            select: {
              id: true,
            },
          },

          updatedAt: true,

          user: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/areaScenarioTriggers")
  @ApiNestedQuery(AreaScenarioTriggerFindManyArgs)
  @GrpcMethod("ScenarioService", "findManyAreaScenarioTriggers")
  async findManyAreaScenarioTriggers(
    @common.Req() request: Request,
    @common.Param() params: ScenarioWhereUniqueInput
  ): Promise<AreaScenarioTrigger[]> {
    const query = plainToClass(AreaScenarioTriggerFindManyArgs, request.query);
    const results = await this.service.findAreaScenarioTriggers(params.id, {
      ...query,
      select: {
        area: {
          select: {
            id: true,
          },
        },

        createdAt: true,
        id: true,
        name: true,

        scenario: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/areaScenarioTriggers")
  @GrpcMethod("ScenarioService", "connectAreaScenarioTriggers")
  async connectAreaScenarioTriggers(
    @common.Param() params: ScenarioWhereUniqueInput,
    @common.Body() body: AreaScenarioTriggerWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      areaScenarioTriggers: {
        connect: body,
      },
    };
    await this.service.updateScenario({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/areaScenarioTriggers")
  @GrpcMethod("ScenarioService", "updateAreaScenarioTriggers")
  async updateAreaScenarioTriggers(
    @common.Param() params: ScenarioWhereUniqueInput,
    @common.Body() body: AreaScenarioTriggerWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      areaScenarioTriggers: {
        set: body,
      },
    };
    await this.service.updateScenario({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/areaScenarioTriggers")
  @GrpcMethod("ScenarioService", "disconnectAreaScenarioTriggers")
  async disconnectAreaScenarioTriggers(
    @common.Param() params: ScenarioWhereUniqueInput,
    @common.Body() body: AreaScenarioTriggerWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      areaScenarioTriggers: {
        disconnect: body,
      },
    };
    await this.service.updateScenario({
      where: params,
      data,
      select: { id: true },
    });
  }
}
