/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { QuestRegionService } from "../questRegion.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { QuestRegionCreateInput } from "./QuestRegionCreateInput";
import { QuestRegion } from "./QuestRegion";
import { QuestRegionFindManyArgs } from "./QuestRegionFindManyArgs";
import { QuestRegionWhereUniqueInput } from "./QuestRegionWhereUniqueInput";
import { QuestRegionUpdateInput } from "./QuestRegionUpdateInput";
import { QuestObjectiveFindManyArgs } from "../../questObjective/base/QuestObjectiveFindManyArgs";
import { QuestObjective } from "../../questObjective/base/QuestObjective";
import { QuestObjectiveWhereUniqueInput } from "../../questObjective/base/QuestObjectiveWhereUniqueInput";
import { QuestPoiFindManyArgs } from "../../questPoi/base/QuestPoiFindManyArgs";
import { QuestPoi } from "../../questPoi/base/QuestPoi";
import { QuestPoiWhereUniqueInput } from "../../questPoi/base/QuestPoiWhereUniqueInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class QuestRegionControllerBase {
  constructor(
    protected readonly service: QuestRegionService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: QuestRegion })
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: QuestRegionCreateInput,
  })
  async createQuestRegion(
    @common.Body() data: QuestRegionCreateInput
  ): Promise<QuestRegion> {
    return await this.service.createQuestRegion({
      data: data,
      select: {
        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [QuestRegion] })
  @ApiNestedQuery(QuestRegionFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async questRegions(@common.Req() request: Request): Promise<QuestRegion[]> {
    const args = plainToClass(QuestRegionFindManyArgs, request.query);
    return this.service.questRegions({
      ...args,
      select: {
        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: QuestRegion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async questRegion(
    @common.Param() params: QuestRegionWhereUniqueInput
  ): Promise<QuestRegion | null> {
    const result = await this.service.questRegion({
      where: params,
      select: {
        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: QuestRegion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  @swagger.ApiBody({
    type: QuestRegionUpdateInput,
  })
  async updateQuestRegion(
    @common.Param() params: QuestRegionWhereUniqueInput,
    @common.Body() data: QuestRegionUpdateInput
  ): Promise<QuestRegion | null> {
    try {
      return await this.service.updateQuestRegion({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: QuestRegion })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteQuestRegion(
    @common.Param() params: QuestRegionWhereUniqueInput
  ): Promise<QuestRegion | null> {
    try {
      return await this.service.deleteQuestRegion({
        where: params,
        select: {
          createdAt: true,
          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/questObjectives")
  @ApiNestedQuery(QuestObjectiveFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "QuestObjective",
    action: "read",
    possession: "any",
  })
  async findQuestObjectives(
    @common.Req() request: Request,
    @common.Param() params: QuestRegionWhereUniqueInput
  ): Promise<QuestObjective[]> {
    const query = plainToClass(QuestObjectiveFindManyArgs, request.query);
    const results = await this.service.findQuestObjectives(params.id, {
      ...query,
      select: {
        createdAt: true,
        id: true,

        questObjectiveCompletionEffects: {
          select: {
            id: true,
          },
        },

        questObjectiveLocales: {
          select: {
            id: true,
          },
        },

        questPois: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/questObjectives")
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "update",
    possession: "any",
  })
  async connectQuestObjectives(
    @common.Param() params: QuestRegionWhereUniqueInput,
    @common.Body() body: QuestObjectiveWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      questObjectives: {
        connect: body,
      },
    };
    await this.service.updateQuestRegion({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/questObjectives")
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "update",
    possession: "any",
  })
  async updateQuestObjectives(
    @common.Param() params: QuestRegionWhereUniqueInput,
    @common.Body() body: QuestObjectiveWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      questObjectives: {
        set: body,
      },
    };
    await this.service.updateQuestRegion({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/questObjectives")
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "update",
    possession: "any",
  })
  async disconnectQuestObjectives(
    @common.Param() params: QuestRegionWhereUniqueInput,
    @common.Body() body: QuestObjectiveWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      questObjectives: {
        disconnect: body,
      },
    };
    await this.service.updateQuestRegion({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id/questPoiPoints")
  @ApiNestedQuery(QuestPoiFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "QuestPoi",
    action: "read",
    possession: "any",
  })
  async findQuestPoiPoints(
    @common.Req() request: Request,
    @common.Param() params: QuestRegionWhereUniqueInput
  ): Promise<QuestPoi[]> {
    const query = plainToClass(QuestPoiFindManyArgs, request.query);
    const results = await this.service.findQuestPoiPoints(params.id, {
      ...query,
      select: {
        createdAt: true,
        id: true,

        pointOfInterest: {
          select: {
            id: true,
          },
        },

        questPoi: {
          select: {
            id: true,
          },
        },

        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/questPoiPoints")
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "update",
    possession: "any",
  })
  async connectQuestPoiPoints(
    @common.Param() params: QuestRegionWhereUniqueInput,
    @common.Body() body: QuestPoiWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      questPoiPoints: {
        connect: body,
      },
    };
    await this.service.updateQuestRegion({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/questPoiPoints")
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "update",
    possession: "any",
  })
  async updateQuestPoiPoints(
    @common.Param() params: QuestRegionWhereUniqueInput,
    @common.Body() body: QuestPoiWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      questPoiPoints: {
        set: body,
      },
    };
    await this.service.updateQuestRegion({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/questPoiPoints")
  @nestAccessControl.UseRoles({
    resource: "QuestRegion",
    action: "update",
    possession: "any",
  })
  async disconnectQuestPoiPoints(
    @common.Param() params: QuestRegionWhereUniqueInput,
    @common.Body() body: QuestPoiWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      questPoiPoints: {
        disconnect: body,
      },
    };
    await this.service.updateQuestRegion({
      where: params,
      data,
      select: { id: true },
    });
  }
}
