/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { View } from "./View";
import { ViewCountArgs } from "./ViewCountArgs";
import { ViewFindManyArgs } from "./ViewFindManyArgs";
import { ViewFindUniqueArgs } from "./ViewFindUniqueArgs";
import { CreateViewArgs } from "./CreateViewArgs";
import { UpdateViewArgs } from "./UpdateViewArgs";
import { DeleteViewArgs } from "./DeleteViewArgs";
import { Fragment } from "../../fragment/base/Fragment";
import { Processor } from "../../processor/base/Processor";
import { ViewService } from "../view.service";
@graphql.Resolver(() => View)
export class ViewResolverBase {
  constructor(protected readonly service: ViewService) {}

  async _viewsMeta(
    @graphql.Args() args: ViewCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [View])
  async views(@graphql.Args() args: ViewFindManyArgs): Promise<View[]> {
    return this.service.views(args);
  }

  @graphql.Query(() => View, { nullable: true })
  async view(@graphql.Args() args: ViewFindUniqueArgs): Promise<View | null> {
    const result = await this.service.view(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => View)
  async createView(@graphql.Args() args: CreateViewArgs): Promise<View> {
    return await this.service.createView({
      ...args,
      data: {
        ...args.data,

        fragment: args.data.fragment
          ? {
              connect: args.data.fragment,
            }
          : undefined,

        processor: args.data.processor
          ? {
              connect: args.data.processor,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => View)
  async updateView(@graphql.Args() args: UpdateViewArgs): Promise<View | null> {
    try {
      return await this.service.updateView({
        ...args,
        data: {
          ...args.data,

          fragment: args.data.fragment
            ? {
                connect: args.data.fragment,
              }
            : undefined,

          processor: args.data.processor
            ? {
                connect: args.data.processor,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => View)
  async deleteView(@graphql.Args() args: DeleteViewArgs): Promise<View | null> {
    try {
      return await this.service.deleteView(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Fragment, {
    nullable: true,
    name: "fragment",
  })
  async getFragment(@graphql.Parent() parent: View): Promise<Fragment | null> {
    const result = await this.service.getFragment(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => Processor, {
    nullable: true,
    name: "processor",
  })
  async getProcessor(
    @graphql.Parent() parent: View
  ): Promise<Processor | null> {
    const result = await this.service.getProcessor(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
