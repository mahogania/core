import * as React from "react";
import {
  Show,
  SimpleShowLayout,
  ShowProps,
  DateField,
  TextField,
  ReferenceField,
} from "react-admin";
import { PARAGRAPHLOCALE_TITLE_FIELD } from "../paragraphLocale/ParagraphLocaleTitle";

export const ParagraphShow = (props: ShowProps): React.ReactElement => {
  return (
    <Show {...props}>
      <SimpleShowLayout>
        <DateField source="createdAt" label="Created At" />
        <TextField label="ID" source="id" />
        <ReferenceField
          label="Paragraph Locales"
          source="paragraphlocale.id"
          reference="ParagraphLocale"
        >
          <TextField source={PARAGRAPHLOCALE_TITLE_FIELD} />
        </ReferenceField>
        <DateField source="updatedAt" label="Updated At" />
      </SimpleShowLayout>
    </Show>
  );
};
