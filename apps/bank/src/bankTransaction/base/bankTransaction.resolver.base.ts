/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { BankTransaction } from "./BankTransaction";
import { BankTransactionCountArgs } from "./BankTransactionCountArgs";
import { BankTransactionFindManyArgs } from "./BankTransactionFindManyArgs";
import { BankTransactionFindUniqueArgs } from "./BankTransactionFindUniqueArgs";
import { CreateBankTransactionArgs } from "./CreateBankTransactionArgs";
import { UpdateBankTransactionArgs } from "./UpdateBankTransactionArgs";
import { DeleteBankTransactionArgs } from "./DeleteBankTransactionArgs";
import { BankTransactionService } from "../bankTransaction.service";
@graphql.Resolver(() => BankTransaction)
export class BankTransactionResolverBase {
  constructor(protected readonly service: BankTransactionService) {}

  async _bankTransactionsMeta(
    @graphql.Args() args: BankTransactionCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [BankTransaction])
  async bankTransactions(
    @graphql.Args() args: BankTransactionFindManyArgs
  ): Promise<BankTransaction[]> {
    return this.service.bankTransactions(args);
  }

  @graphql.Query(() => BankTransaction, { nullable: true })
  async bankTransaction(
    @graphql.Args() args: BankTransactionFindUniqueArgs
  ): Promise<BankTransaction | null> {
    const result = await this.service.bankTransaction(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => BankTransaction)
  async createBankTransaction(
    @graphql.Args() args: CreateBankTransactionArgs
  ): Promise<BankTransaction> {
    return await this.service.createBankTransaction({
      ...args,
      data: args.data,
    });
  }

  @graphql.Mutation(() => BankTransaction)
  async updateBankTransaction(
    @graphql.Args() args: UpdateBankTransactionArgs
  ): Promise<BankTransaction | null> {
    try {
      return await this.service.updateBankTransaction({
        ...args,
        data: args.data,
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => BankTransaction)
  async deleteBankTransaction(
    @graphql.Args() args: DeleteBankTransactionArgs
  ): Promise<BankTransaction | null> {
    try {
      return await this.service.deleteBankTransaction(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
